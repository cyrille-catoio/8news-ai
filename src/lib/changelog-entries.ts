export interface ChangelogEntryDef {
  version: string;
  title_en: string;
  title_fr: string;
  body_en: string;
  body_fr: string;
  created_at: string;
}

export const CHANGELOG_ENTRIES: ChangelogEntryDef[] = [
  {
    version: "2.5.4",
    title_en: "v2.5.4: Pre-warm cron upgrades to gpt-5.3-chat-latest — sharper video summaries",
    title_fr: "v2.5.4 : Le cron de pré-chauffe passe à gpt-5.3-chat-latest — résumés vidéo plus fins",
    body_en:
      "**Why this change.** The video summary pipeline (`src/lib/transcribe-video.ts`) was the last surface still pinned on `gpt-4.1-mini` while the editorial flagship roundups (`generate-video-roundup.ts`) and the daily Top 50 (`/api/news/top-summary`) had already moved to **`gpt-5.3-chat-latest`**. Pulling per-video summaries up to the same quality bar — without compromising the synchronous on-demand button's UX guarantee — was the goal.\n\n**Hybrid model strategy.** A naive « switch the constant » would have re-introduced 504s on long podcasts via the `/api/youtube-channels/transcribe` route, which is bound by Netlify's hard 30 s synchronous-function budget. Instead:\n\n- **Synchronous route (fallback for very-fresh videos)** stays on `gpt-4.1-mini`, default kept as `DEFAULT_AI_MODEL`. Predictable < 30 s, zero regression risk on the on-demand button. This path now serves only the ~5 % of videos published in the last 15 min before any cron tick has had a chance to pre-warm them.\n- **Background cron (`cron-video-transcribe-background`)** opts into `gpt-5.3-chat-latest` via the new `model` field on `TranscribeOptions`. Background functions have a 15 min wall budget, so the slower model is fine. Roughly 95 %+ of summaries a real visitor sees on `/app/videos`, `/app`, `/{topic}/v/...` and the per-video SSR pages now come from this higher-quality path.\n\n**Code changes.** `src/lib/transcribe-video.ts` — renamed the constant `AI_MODEL` → `DEFAULT_AI_MODEL` to make the override semantics explicit, added `model?: string` to `TranscribeOptions`, threaded `opts.model ?? DEFAULT_AI_MODEL` through both the translate path and the full pipeline OpenAI call. `cron-video-transcribe-background.ts` — declared `CRON_AI_MODEL = \"gpt-5.3-chat-latest\"`, bumped `CRON_OPENAI_TIMEOUT_MS` 90 s → **180 s** to comfortably accommodate the slower model on long-form content (typical 5.3 latency on 50K-char transcripts: 40-90 s with occasional spikes), bumped `SAFETY_MS` 30 s → **200 s** so the budget guard never starts a transcribe it can't finish before the wall deadline.\n\n**Trade-offs accepted.** Per-bucket cost rises ~10-20× on the cron path, but the daily volume is small (typically 50-100 buckets/day once the queue is drained, capped at 40 per tick via `TRANSCRIBE_MAX_PER_RUN`). With 96 ticks/day, real-world throughput stays comfortable: roughly 3-6 transcribes per tick × 96 ticks = far more headroom than the actual demand. The translate path inside the same lib also benefits — once the alt-lang cache row is filled by the full pipeline, the second lang takes ~15-25 s on 5.3 (vs ~8 s on 4.1-mini), still well under the 180 s timeout.\n\n**Backward compat.** Existing cache rows in `video_transcriptions` are NOT regenerated — the `getVideoTranscription` fast-path returns them unchanged. Quality lift only kicks in for new buckets going forward. To regenerate a specific row at the new quality: delete it manually in Supabase, the next cron tick (or an on-demand click) will re-summarize.\n\n**Bonus — landing pricing tidy-up.** Same release ships a Pro-tier annual pricing line (**88 € /year, -8 %**) rendered to the right of the monthly price via a new `.price-row` flex container, plus a features pass on both tiers (Free: « 8 topics out of 36 » phrasing, merged Top 50 / favorites line, dropped ElevenLabs row; Pro: dropped Webhooks/API + scoring queue rows, « Morning email digest covering all your topics. »).\n\n**Release.** Bump 2.5.3 → 2.5.4 via `npm run release:patch`. No DB migration required.",
    body_fr:
      "**Pourquoi ce changement.** Le pipeline de résumé vidéo (`src/lib/transcribe-video.ts`) était la dernière surface encore figée sur `gpt-4.1-mini` alors que les roundups éditoriaux flagship (`generate-video-roundup.ts`) et le Top 50 quotidien (`/api/news/top-summary`) étaient déjà passés à **`gpt-5.3-chat-latest`**. L'objectif : remonter les résumés per-vidéo au même niveau de qualité — sans casser la garantie UX du bouton synchrone à la demande.\n\n**Stratégie hybride.** Un naïf « on switch la constante » aurait ré-introduit des 504 sur les longs podcasts via la route `/api/youtube-channels/transcribe`, contrainte par le budget dur de 30 s des functions synchrones Netlify. À la place :\n\n- **Route synchrone (fallback pour les vidéos toutes fraîches)** reste sur `gpt-4.1-mini`, défaut gardé en `DEFAULT_AI_MODEL`. Prévisible < 30 s, zéro risque de régression sur le bouton à la demande. Ce chemin ne sert plus que les ~5 % de vidéos publiées dans les 15 min avant qu'un tick de cron ait eu sa chance de les pré-chauffer.\n- **Cron background (`cron-video-transcribe-background`)** opt-in `gpt-5.3-chat-latest` via le nouveau champ `model` sur `TranscribeOptions`. Les background functions ont 15 min de budget mur, donc le modèle plus lent passe sans souci. À peu près 95 %+ des résumés vus par un vrai visiteur sur `/app/videos`, `/app`, `/{topic}/v/...` et les pages SSR per-vidéo viennent désormais de ce chemin plus qualitatif.\n\n**Modifs code.** `src/lib/transcribe-video.ts` — la constante `AI_MODEL` est renommée `DEFAULT_AI_MODEL` pour expliciter la sémantique d'override, `model?: string` ajouté à `TranscribeOptions`, `opts.model ?? DEFAULT_AI_MODEL` câblé dans les deux appels OpenAI (path translate + pipeline complet). `cron-video-transcribe-background.ts` — déclaration `CRON_AI_MODEL = \"gpt-5.3-chat-latest\"`, bump `CRON_OPENAI_TIMEOUT_MS` 90 s → **180 s** pour confortablement encaisser la latence du modèle plus lent sur du long-form (latence typique de 5.3 sur 50K chars : 40-90 s avec spikes occasionnels), bump `SAFETY_MS` 30 s → **200 s** pour que le garde-fou de budget ne lance jamais un transcribe qu'il ne pourra pas finir avant la deadline mur.\n\n**Trade-offs assumés.** Le coût par bucket monte ~10-20× sur le path cron, mais le volume quotidien reste petit (typiquement 50-100 buckets/jour une fois la file drainée, capé à 40 par tick via `TRANSCRIBE_MAX_PER_RUN`). Avec 96 ticks/jour, le throughput réel reste très confortable : environ 3-6 transcribes par tick × 96 ticks = beaucoup plus de marge que la demande réelle. Le path translate à l'intérieur de la même lib bénéficie aussi du changement — une fois la ligne cache alt-lang remplie par le pipeline complet, la 2e langue prend ~15-25 s en 5.3 (vs ~8 s en 4.1-mini), toujours largement sous le timeout 180 s.\n\n**Compat ascendante.** Les lignes cache existantes dans `video_transcriptions` ne sont PAS régénérées — le fast-path `getVideoTranscription` les renvoie inchangées. Le gain qualité ne s'applique qu'aux nouveaux buckets à partir de maintenant. Pour régénérer une ligne précise à la nouvelle qualité : la delete à la main dans Supabase, le tick de cron suivant (ou un clic à la demande) la re-résumera.\n\n**Bonus — toilettage du pricing landing.** La même release embarque un prix annuel pour le Pro (**88 €/an, -8 %**) rendu à droite du prix mensuel via un nouveau container flex `.price-row`, plus une passe sur les features des deux paliers (Gratuit : reformulation « 8 topics parmi les 36 », fusion ligne Top 50 / favoris, suppression de la ligne ElevenLabs ; Pro : suppression des lignes Webhooks/API + file de scoring, « Digest matinal reprenant tous vos topics. »).\n\n**Release.** Bump 2.5.3 → 2.5.4 via `npm run release:patch`. Pas de migration DB.",
    created_at: "2026-04-25T21:30:00Z",
  },
  {
    version: "2.5.3",
    title_en: "v2.5.3: Language sticks to the user — no more silent EN flip on /briefings",
    title_fr: "v2.5.3 : La langue suit l'utilisateur — fini le flip silencieux vers EN sur /briefings",
    body_en:
      "**The bug.** A FR-speaking visitor (signed in or not) clicking « Briefings vidéo » in the menu landed on `/briefings` rendered in **English**. Same on `/summaries` and on `/[topic]` whenever the link came in without a `?lang=fr` query string. Reason: those SSR pages defaulted to `en` whenever the URL had no explicit lang param, completely ignoring both the user's preference and the cookie the SPA had been writing for them.\n\n**The fix — a single resolution helper, used by every SSR entry point.** New `src/lib/server-lang.ts` exports `resolveServerLang(explicitLang, defaultLang = \"en\")`. Order:\n\n1. `?lang=fr|en` (explicit override — keeps shared links in a given language deterministic regardless of who's reading).\n2. **`auth.users.raw_user_meta_data.preferred_lang`** for the signed-in user (read via Supabase SSR client and the same auth cookies the API routes already use).\n3. Cookie `lang` for anonymous visitors.\n4. Caller-provided default — `en` for SEO pages, `fr` for the landing.\n\nWired into `/briefings`, `/summaries`, `/[topic]` (both `generateMetadata` and the page component) and `/` (landing keeps its FR fallback). The `/[topic]/v/...` and `/[topic]/r/...` pages are NOT touched — their language comes from the row stored in the DB, not from the visitor's preference.\n\n**SPA `/app` aligned on the same model.** Root client component now declares `useAuth()` early and runs a sync effect on the auth session: when the session resolves, the user's `user_metadata.preferred_lang` wins over whatever cookie was used during the bootstrap. Legacy users without a `preferred_lang` are auto-seeded with the cookie value on first visit so the field is always set going forward. `handleLangChange` writes BOTH the cookie (immediate, picked up by next SSR request) AND `user_metadata.preferred_lang` via `supabase.auth.updateUser` (async, fire-and-forget) so the choice survives clearing cookies / switching browsers.\n\n**SeoNavBar `LangToggle` now persists before navigating.** Before, clicking « FR » on a SSR page just navigated to `?lang=fr` — the user got that page in FR but the next click anywhere brought them right back to EN since neither the cookie nor the metadata had been touched. The toggle now intercepts the click, writes the cookie synchronously, fires `auth.updateUser({ data: { preferred_lang } })` for signed-in users, then navigates. The `?lang=` param remains as a last-ditch override but is no longer load-bearing.\n\n**Storage choice.** No DB migration required: `auth.users.raw_user_meta_data` is already a JSONB column owned by Supabase auth, accessible from both SSR (`supabase.auth.getUser()`) and the browser (`session.user.user_metadata`). Adding a dedicated `users.preferred_lang` column would have been overkill for a single-key scalar that auth already gives us for free.\n\n**Release.** Bump 2.5.2 → 2.5.3 via `npm run release:patch`. No DB migration required.",
    body_fr:
      "**Le bug.** Un visiteur FR (connecté ou non) qui cliquait sur « Briefings vidéo » dans le menu atterrissait sur `/briefings` affiché en **anglais**. Idem sur `/summaries` et sur `/[topic]` dès que le lien arrivait sans `?lang=fr` dans l'URL. Cause : ces pages SSR tombaient par défaut sur `en` dès que l'URL n'avait pas de param lang explicite, ignorant complètement la préférence du user et le cookie que la SPA avait écrit pour lui.\n\n**Le fix — un seul helper de résolution, utilisé par chaque entry point SSR.** Nouveau `src/lib/server-lang.ts` qui expose `resolveServerLang(explicitLang, defaultLang = \"en\")`. Ordre :\n\n1. `?lang=fr|en` (override explicite — garde les liens partagés dans une langue donnée déterministes peu importe qui lit).\n2. **`auth.users.raw_user_meta_data.preferred_lang`** pour le user connecté (lu via le client Supabase SSR avec les mêmes cookies d'auth que les routes API utilisent déjà).\n3. Cookie `lang` pour les visiteurs anonymes.\n4. Default fourni par le caller — `en` pour les pages SEO, `fr` pour la landing.\n\nBranché sur `/briefings`, `/summaries`, `/[topic]` (à la fois `generateMetadata` et le composant page) et `/` (la landing garde son fallback FR). Les pages `/[topic]/v/...` et `/[topic]/r/...` ne sont PAS touchées — leur langue vient de la ligne stockée en BDD, pas de la préférence du visiteur.\n\n**SPA `/app` alignée sur le même modèle.** Le composant client racine déclare désormais `useAuth()` tôt et fait tourner un effect de sync sur la session auth : quand la session arrive, le `user_metadata.preferred_lang` du user gagne sur le cookie utilisé au bootstrap. Les comptes legacy sans `preferred_lang` sont auto-seed avec la valeur du cookie à la première visite — comme ça le champ est toujours rempli ensuite. `handleLangChange` écrit BOTH le cookie (immédiat, picked up par la prochaine requête SSR) ET `user_metadata.preferred_lang` via `supabase.auth.updateUser` (async, fire-and-forget) — la préférence survit donc à un clear-cookies / un changement de navigateur.\n\n**SeoNavBar `LangToggle` persiste maintenant avant de naviguer.** Avant, cliquer « FR » sur une page SSR naviguait juste vers `?lang=fr` — le user obtenait cette page en FR, mais le clic suivant n'importe où le ramenait direct en EN puisque ni le cookie ni le metadata n'avaient été touchés. Le toggle intercepte désormais le clic, écrit le cookie synchroniquement, déclenche `auth.updateUser({ data: { preferred_lang } })` pour les users connectés, puis navigue. Le param `?lang=` reste comme override de dernier recours mais n'est plus structurel.\n\n**Choix de stockage.** Pas de migration DB : `auth.users.raw_user_meta_data` est déjà une colonne JSONB owned par Supabase auth, accessible depuis le SSR (`supabase.auth.getUser()`) comme depuis le navigateur (`session.user.user_metadata`). Ajouter une colonne dédiée `users.preferred_lang` aurait été overkill pour un scalaire single-key que l'auth nous donne gratuit.\n\n**Release.** Bump 2.5.2 → 2.5.3 via `npm run release:patch`. Pas de migration DB.",
    created_at: "2026-04-25T11:45:00Z",
  },
  {
    version: "2.5.2",
    title_en: "v2.5.2: Briefing « All transcribed videos » paginated 1 day at a time (today first)",
    title_fr: "v2.5.2 : Briefing « Toutes les vidéos transcrites » paginé jour par jour (aujourd'hui d'abord)",
    body_en:
      "**One day per page, today by default.** The « Toutes les vidéos transcrites » block at the bottom of `/app` (Briefing homepage) was paginated by 2-day chunks in v2.5.1 (today + yesterday on page 0), which felt off — the block is supposed to be a « what's new today » entry point, not a rolling 2-day window. It now shows **exactly one calendar day per page**, with page 0 = today, page 1 = yesterday, page 2 = the day before, etc. « Plus ancien → » walks back one day at a time, « ← Plus récent » brings the user back towards today.\n\n**`/api/video-pages/recent` tweaks.** `PAGE_SIZE_DAYS` 2 → **1**, `MAX_PAGE` 30 → **60** (still 60 days of archive, just one page per day now). The response shape (`{ items, page, pageSizeDays, fromDate, toDate, hasMore }`) is unchanged — `fromDate === toDate` on every page since each page covers a single day. The `hasMore` probe still keys off `published_date < fromDate` so the « Plus ancien » button stays accurate.\n\n**Section never disappears when archive exists.** Previously, page 0 with zero transcribed videos hid the entire section — even if older days had content. Now the section stays rendered as long as the server reports `hasMore: true`, with a friendly empty state (« Aucune vidéo transcrite aujourd'hui pour le moment ») and the « Plus ancien → » button enabled, so the user can always walk back to find content. Only when both today AND the archive are empty do we hide the section completely.\n\n**Empty-state copy split.** Page 0 says « Aucune vidéo transcrite aujourd'hui pour le moment » / « No transcribed videos yet today », every other page says « Aucune vidéo transcrite ce jour-là » / « No transcribed videos for this day ». The phrasing now correctly distinguishes « not yet processed » (today) from « historically empty » (past day).\n\n**Bonus — quieter cron logs when migration 018 is missing.** `insertVideoRoundupBullets` now detects the « missing column » PostgREST error (`PGRST204` / PG `42703` / `...schema cache` message) and downgrades it from `ERROR` to `WARN` with a single actionable line: « run migration 018-roundup-bullets.sql in Supabase to enable the bullets mirror ». The roundup itself was always persisted correctly even when the mirror failed (mirror is intentionally non-fatal); this just stops polluting the Netlify error dashboards with a recurring red line on every cron tick until the migration is applied.\n\n**Release.** Bump 2.5.1 → 2.5.2 via `npm run release:patch`. No DB migration required (though migration 018 from v2.4 is still recommended to enable the summary_bullets mirror).",
    body_fr:
      "**Un jour par page, aujourd'hui par défaut.** Le bloc « Toutes les vidéos transcrites » en bas de `/app` (home Briefing) était paginé par tranches de 2 jours en v2.5.1 (aujourd'hui + hier sur la page 0), ce qui sonnait faux — le bloc est censé être une entrée « quoi de neuf aujourd'hui », pas une fenêtre glissante de 2 jours. Il affiche désormais **exactement un jour calendaire par page**, avec page 0 = aujourd'hui, page 1 = hier, page 2 = avant-hier, etc. « Plus ancien → » remonte d'un jour à la fois, « ← Plus récent » ramène l'utilisateur vers aujourd'hui.\n\n**Tweaks `/api/video-pages/recent`.** `PAGE_SIZE_DAYS` 2 → **1**, `MAX_PAGE` 30 → **60** (toujours 60 jours d'archive, juste une page par jour maintenant). La shape de la réponse (`{ items, page, pageSizeDays, fromDate, toDate, hasMore }`) est inchangée — `fromDate === toDate` sur chaque page puisque chaque page couvre un seul jour. La probe `hasMore` reste sur `published_date < fromDate`, le bouton « Plus ancien » reste donc précis.\n\n**La section ne disparaît plus quand l'archive existe.** Avant, page 0 avec zéro vidéo transcrite cachait toute la section — même si les jours plus anciens avaient du contenu. Désormais la section reste rendue tant que le serveur renvoie `hasMore: true`, avec un empty-state (« Aucune vidéo transcrite aujourd'hui pour le moment ») et le bouton « Plus ancien → » activé, pour que l'utilisateur puisse toujours remonter chercher du contenu. La section n'est masquée complètement que si aujourd'hui ET l'archive sont vides.\n\n**Copy empty-state séparé.** Page 0 dit « Aucune vidéo transcrite aujourd'hui pour le moment », toutes les autres pages disent « Aucune vidéo transcrite ce jour-là ». La formulation distingue maintenant correctement « pas encore traitée » (aujourd'hui) de « historiquement vide » (jour passé).\n\n**Bonus — logs cron plus calmes quand la migration 018 manque.** `insertVideoRoundupBullets` détecte désormais l'erreur PostgREST « colonne absente » (`PGRST204` / PG `42703` / message `...schema cache`) et la downgrade de `ERROR` à `WARN` avec une ligne actionnable unique : « run migration 018-roundup-bullets.sql in Supabase to enable the bullets mirror ». Le roundup lui-même était toujours persisté correctement même quand le miroir échouait (le miroir est volontairement non-fatal) ; on arrête juste de polluer les dashboards d'erreurs Netlify avec une ligne rouge récurrente à chaque tick de cron tant que la migration n'est pas appliquée.\n\n**Release.** Bump 2.5.1 → 2.5.2 via `npm run release:patch`. Pas de migration DB obligatoire (la migration 018 de v2.4 reste recommandée pour activer le miroir summary_bullets).",
    created_at: "2026-04-25T08:30:00Z",
  },
  {
    version: "2.5.1",
    title_en: "v2.5.1: Briefing « All transcribed videos » paginated by 2-day chunks",
    title_fr: "v2.5.1 : Briefing « Toutes les vidéos transcrites » paginé par tranches de 2 jours",
    body_en:
      "**Pagination on the homepage video list.** The « Toutes les vidéos transcrites · 7 derniers jours » block at the bottom of `/app` (Briefing homepage) used to dump up to 100 rows from the last 7 days into a single card — too long once the auto-transcribe cron started filling things up. It's now paginated by **2-day chunks**, controlled by Prev/Next buttons:\n\n- Page 0 → today + yesterday (default)\n- Page 1 → 2 and 3 days ago\n- Page 2 → 4 and 5 days ago\n- … up to page 30 (60 days back)\n\n**`/api/video-pages/recent` rewritten.** New shape: `?page=N&lang=...` returns `{ items, page, pageSizeDays, fromDate, toDate, hasMore }`. `hasMore` is computed via a parallel HEAD count probe on `published_date < fromDate` so the « Plus ancien » button can be disabled without a second client roundtrip. Each `(page, lang)` combo gets its own edge cache entry (still 60 s + SWR 300 s).\n\n**UX details.** Header has a date-range subtitle (« 23 – 24 avr. ») that refreshes per page. Loader (« Chargement… ») and empty state (« Aucune vidéo transcrite sur cette période ») both inline. Buttons : « ← Plus récent » (disabled at page 0), page indicator, « Plus ancien → » (disabled when `hasMore = false`). Reset to page 0 on FR ↔ EN toggle so a lang switch never lands the user on an empty page.\n\n**Release.** Bump 2.5.0 → 2.5.1 via `npm run release:patch`. No DB migration required.",
    body_fr:
      "**Pagination sur la liste vidéo de la home.** Le bloc « Toutes les vidéos transcrites · 7 derniers jours » en bas de `/app` (home Briefing) dumpait jusqu'à 100 lignes des 7 derniers jours dans une seule card — trop long une fois que le cron d'auto-transcription a commencé à charger. Il est maintenant paginé par **tranches de 2 jours**, contrôlé par des boutons Précédent/Suivant :\n\n- Page 0 → aujourd'hui + hier (défaut)\n- Page 1 → J-2 et J-3\n- Page 2 → J-4 et J-5\n- … jusqu'à la page 30 (60 jours en arrière)\n\n**`/api/video-pages/recent` réécrit.** Nouvelle shape : `?page=N&lang=...` renvoie `{ items, page, pageSizeDays, fromDate, toDate, hasMore }`. `hasMore` est calculé via une probe HEAD count parallèle sur `published_date < fromDate` pour que le bouton « Plus ancien » puisse être disabled sans un 2e aller-retour client. Chaque combinaison `(page, lang)` a son propre cache edge (toujours 60 s + SWR 300 s).\n\n**Détails UX.** Header avec un sous-titre de date-range (« 23 – 24 avr. ») qui rafraîchit par page. Loader (« Chargement… ») et état vide (« Aucune vidéo transcrite sur cette période ») inline. Boutons : « ← Plus récent » (disabled à page 0), indicateur de page, « Plus ancien → » (disabled quand `hasMore = false`). Reset à page 0 quand on toggle FR ↔ EN pour qu'un changement de langue n'atterrisse jamais sur une page vide.\n\n**Release.** Bump 2.5.0 → 2.5.1 via `npm run release:patch`. Pas de migration DB.",
    created_at: "2026-04-24T19:00:00Z",
  },
  {
    version: "2.5",
    title_en: "v2.5: Auto-transcribe cron — instant summaries on /app/videos",
    title_fr: "v2.5 : Cron d'auto-transcription — résumés instantanés sur /app/videos",
    body_en:
      "**New 15-minute background function.** `cron-video-transcribe-background` pre-warms the AI summary cache for every transcribable video published in the last 24 h. By the time a visitor lands on `/app/videos`, the « Résumé » button is mostly a cache-hit (response < 1 s, no spinner). The synchronous on-demand button stays in place for very-fresh videos that haven't been picked up yet by a tick.\n\n**Both languages, automatically.** Each candidate video is transcribed in EN AND FR — the first lang runs the full pipeline (~25 s), the second hits the existing translate path (~8 s) since the alt-lang cache row now exists. So whoever arrives — EN or FR visitor — gets an instant summary.\n\n**Shorts skipped.** Same default as the SPA's « Vidéos du jour » filter: videos under 120 s are excluded. Saves OpenAI tokens on low-value content. Missing durations are backfilled via the YouTube Data API v3 (the same `enrichDurations` helper now extracted to `src/lib/youtube-duration.ts` and shared between the SPA route and the cron).\n\n**Architecture refactor (non-breaking).** The transcription pipeline (cache check → translate path or full path → slug + DB persist) was extracted from `/api/youtube-channels/transcribe` into a new `src/lib/transcribe-video.ts`. The HTTP route is now a 30-line shell that delegates to `transcribeVideo()` with the same response shape and HTTP status codes as before (200 / 400 / 404 / 429 / 500 / 502 / 504). The cron consumes the same lib with a 90 s OpenAI timeout (vs 25 s for the synchronous route) so very long podcasts that would 504 the synchronous button now have a chance to complete.\n\n**Skip-if-exists pattern.** Single bulk SELECT on `video_transcriptions(video_id, lang)` per tick builds a `Set<videoId|lang>` of already-done buckets — no per-bucket cache check inside the loop. Same pattern as `cron-video-roundup-background`. `MAX_BUCKETS_PER_RUN = 40` (tunable via `TRANSCRIBE_MAX_PER_RUN` env var) caps OpenAI spend per tick. With 96 ticks/day, real-world load lands around 50-100 buckets/day total once the queue is drained.\n\n**Setup required (action utilisateur).** Add a job on cron-job.org pointing at `https://8news.ai/.netlify/functions/cron-video-transcribe-background`, POST, schedule `*/15 * * * *` (every 15 min). Background functions return 202 immediately — make sure cron-job.org accepts that as success.\n\n**Release.** Bump 2.4.1 → 2.5.0 via `npm run release:minor`. No DB migration required.",
    body_fr:
      "**Nouvelle background function 15 minutes.** `cron-video-transcribe-background` pré-chauffe le cache des résumés IA pour chaque vidéo transcribable publiée dans les 24 h. Quand un visiteur arrive sur `/app/videos`, le bouton « Résumé » est désormais quasi-toujours un cache-hit (réponse < 1 s, plus de spinner). Le bouton synchrone à la demande reste en place pour les vidéos toutes fraîches qui n'ont pas encore été pickées par un tick.\n\n**Les 2 langues, automatiquement.** Chaque vidéo candidate est transcrite en EN ET en FR — la première langue lance le pipeline complet (~25 s), la seconde hit le path translate existant (~8 s) puisque la ligne alt-lang du cache existe désormais. Quel que soit le visiteur (EN ou FR), il a son résumé instantané.\n\n**Shorts skip.** Même défaut que le filtre « Vidéos du jour » de la SPA : les vidéos sous 120 s sont exclues. Économise les tokens OpenAI sur du contenu à faible valeur. Les durées manquantes sont backfillées via la YouTube Data API v3 (le même helper `enrichDurations` désormais extrait dans `src/lib/youtube-duration.ts` et partagé entre la route SPA et le cron).\n\n**Refactor d'architecture (non-breaking).** Le pipeline de transcription (cache check → path translate ou path full → slug + DB persist) a été extrait de `/api/youtube-channels/transcribe` dans un nouveau `src/lib/transcribe-video.ts`. La route HTTP devient une coquille de 30 lignes qui délègue à `transcribeVideo()` avec exactement la même shape de réponse et les mêmes codes HTTP qu'avant (200 / 400 / 404 / 429 / 500 / 502 / 504). Le cron consomme la même lib avec un timeout OpenAI de 90 s (vs 25 s pour la route synchrone) — les podcasts très longs qui 504-aient le bouton synchrone ont maintenant une chance d'aboutir.\n\n**Pattern skip-if-exists.** Un seul bulk SELECT sur `video_transcriptions(video_id, lang)` par tick construit un `Set<videoId|lang>` des buckets déjà faits — pas de cache check par bucket dans la boucle. Même pattern que `cron-video-roundup-background`. `MAX_BUCKETS_PER_RUN = 40` (tunable via la variable d'env `TRANSCRIBE_MAX_PER_RUN`) cape la dépense OpenAI par tick. Avec 96 ticks/jour, la charge réelle se stabilise autour de 50-100 buckets/jour total une fois la file drainée.\n\n**Setup requis (action utilisateur).** Ajouter un job sur cron-job.org pointant sur `https://8news.ai/.netlify/functions/cron-video-transcribe-background`, POST, schedule `*/15 * * * *` (toutes les 15 min). Les background functions renvoient 202 immédiatement — vérifier que cron-job.org accepte 202 comme un succès.\n\n**Release.** Bump 2.4.1 → 2.5.0 via `npm run release:minor`. Pas de migration DB.",
    created_at: "2026-04-23T18:00:00Z",
  },
  {
    version: "2.4.1",
    title_en: "v2.4.1: Video roundup pulls a 48 h source window (was 24 h), SSR fetches videos by id",
    title_fr: "v2.4.1 : Le roundup vidéo tire ses sources sur 48 h (vs 24 h), la page SSR fetche par id",
    body_en:
      "**Wider source window — denser briefings.** The nightly cron `cron-video-roundup-background` used to pull only videos with `published_date = yesterday` for each roundup, which made for thin briefings on slow news days (especially per-topic, where 1-2 transcribed videos in a 24 h window is common). The roundup is still KEYED to `roundup_date = yesterday` (the unique constraint stays valid), but `generateVideoRoundup` now pulls source material from a **48 h window** ending at end-of-day yesterday, i.e. `published_date IN [day-before-yesterday, yesterday]` inclusive. New `WINDOW_DAYS = 2` constant in `src/lib/generate-video-roundup.ts`.\n\n**`getVideoTranscriptionsForRoundup` signature widened.** Now takes `(topicId, fromDate, toDate, lang)` — pass the same value for both bounds for the legacy single-day behaviour. Range is inclusive on both sides on `published_date`.\n\n**SSR `/r/` page rebuilt around `video_ids`.** Critical fix that ships with the window widening: the page used to fetch by `(topicId, roundup_date, lang)` and re-sort by `roundup.video_ids`. With a 48 h window, video_ids now contains rows from `roundup_date - 1`, which a single-day fetch silently drops. The page now calls a new `getVideoTranscriptionsByIds(roundup.video_ids, lang)` helper that hydrates the EXACT id list — faithful to whatever window the generator used at write-time. Per-video card links + JSON-LD ItemList URLs use `v.published_date` (with `roundup_date` fallback) instead of the roundup_date so they keep pointing at the real `/v/{date}/{slug}` SSR pages.\n\n**Section copy.** « Les vidéos du jour » / « Today's videos » → **« Vidéos couvertes » / « Videos covered »** to reflect that the cards are no longer all from the same calendar day.\n\n**Backward compat.** Old roundups (`video_ids` only spans 1 day) render identically — fetching by IDs returns the same set as the old single-day fetch did. To regenerate any existing roundup with the wider 48 h window: `POST /api/roundups/generate { topicId, date, lang }` (the cron stays skip-if-exists).\n\n**Release.** Bump 2.4.0 → 2.4.1 via `npm run release:patch`. No migration to run.",
    body_fr:
      "**Fenêtre source élargie — briefings plus denses.** Le cron nocturne `cron-video-roundup-background` ne tirait jusqu'ici que les vidéos avec `published_date = yesterday` pour chaque roundup, ce qui produisait des briefings maigres les jours creux (surtout par topic, où 1-2 vidéos transcrites sur 24 h est fréquent). Le roundup reste KEYED sur `roundup_date = yesterday` (le unique constraint reste valide), mais `generateVideoRoundup` tire désormais ses sources sur une **fenêtre de 48 h** finissant à la fin de yesterday, soit `published_date IN [avant-hier, yesterday]` inclusif. Nouvelle constante `WINDOW_DAYS = 2` dans `src/lib/generate-video-roundup.ts`.\n\n**Signature `getVideoTranscriptionsForRoundup` élargie.** Prend maintenant `(topicId, fromDate, toDate, lang)` — passer la même valeur des deux côtés pour retomber sur le single-day legacy. Range inclusif sur les deux bornes via `published_date`.\n\n**Page SSR `/r/` refondue autour de `video_ids`.** Fix critique qui accompagne l'élargissement : la page fetchait par `(topicId, roundup_date, lang)` et re-sortait par `roundup.video_ids`. Avec une fenêtre 48 h, video_ids contient maintenant des lignes du `roundup_date - 1`, qu'un fetch single-day drop silencieusement. La page appelle désormais un nouveau helper `getVideoTranscriptionsByIds(roundup.video_ids, lang)` qui hydrate EXACTEMENT la liste d'IDs — fidèle à la fenêtre que le générateur a utilisée à l'écriture. Les liens des cards per-vidéo + les URLs du JSON-LD ItemList utilisent `v.published_date` (fallback sur `roundup_date`) plutôt que le roundup_date pour pointer vers les vraies pages SSR `/v/{date}/{slug}`.\n\n**Copy section.** « Les vidéos du jour » / « Today's videos » → **« Vidéos couvertes » / « Videos covered »** pour refléter que les cards ne sont plus toutes du même jour calendaire.\n\n**Compat ascendante.** Les anciens roundups (`video_ids` ne couvrant qu'1 jour) s'affichent à l'identique — fetch par IDs renvoie le même set que le fetch single-day. Pour régénérer un roundup existant avec la nouvelle fenêtre 48 h : `POST /api/roundups/generate { topicId, date, lang }` (le cron reste skip-if-exists).\n\n**Release.** Bump 2.4.0 → 2.4.1 via `npm run release:patch`. Pas de migration à jouer.",
    created_at: "2026-04-23T16:00:00Z",
  },
  {
    version: "2.4",
    title_en: "v2.4: Video briefings rebuilt — 8 structured bullets, gpt-5.3-chat-latest, summary_bullets mirror",
    title_fr: "v2.4 : Briefings vidéo refondés — 8 bullets structurés, gpt-5.3-chat-latest, miroir summary_bullets",
    body_en:
      "**New briefing format.** The per-topic-per-day video roundup at `/{topic}/r/{date}/{slug}` (rendered from `video_roundups.intro_md`) used to be a single one-paragraph intro of 3-5 sentences — too thin for an editorial product. It's now a structured **8-bullet briefing**, each bullet with:\n\n- A **journalistic title** (3-8 words, anchored on a proper noun, product or key figure — e.g. \"GPT-5 jumps to 2M tokens\")\n- A **body of 3-5 sentences** (target 4) with **bold** for proper nouns / numbers / dates, written in dense editorial press style. Never references the source videos / channels / speakers — pure subject-first journalism.\n- 8 distinct angles, no redundancy.\n\nStored as a sequence of `### Title\\n\\nbody...` Markdown in the existing `video_roundups.intro_md` column (no schema change for the roundup itself), rendered with a custom `h3` style (gold, 17px, tight to body) on the SSR page. Added a small \"Briefing\" kicker above the box so the new structure is visually labelled.\n\n**SEO tightened.** The prompt now bans generic phrasing in `seo_title` (\"Daily tech briefing\"), enforces 5-7 specific kebab-case keywords in `slug_keywords` (forbidden words: `news`, `briefing`, `daily`, `ai`, `tech`, `video`, `today`), and demands the `seo_description` contain ≥ 3 specific terms (product names, figures, companies). Concrete examples baked in for both EN + FR (\"Sora 2 vs Veo 3: OpenAI strikes back with native camera mode\" / \"GPT-5 passe à 2M de contexte, Anthropic réplique avec Claude 4.6\").\n\n**Best model in town.** `gpt-4.1-mini` → **`gpt-5.3-chat-latest`** (same model the `/api/news/top-summary` endpoint already uses for top-50 summaries). The roundup is the editorial flagship and the cron is a Netlify background function (15 min budget) so latency is irrelevant; quality is everything. `OPENAI_TIMEOUT_MS` bumped 25s → **60s** to fit the slower (but smarter) model comfortably.\n\n**summary_bullets mirror.** Migration **018** adds `summary_bullets.video_roundup_id INT REFERENCES video_roundups(id) ON DELETE CASCADE` (+ index). On every roundup write the 8 bullets are also persisted as 8 rows in `summary_bullets` with `source_type = 'video_roundup'`, `text = '**Title**\\n\\nBody'`, `bullet_index = 0..7`. This is the same pattern already used for `article` / `video` / `top50` source types — bullets become uniformly queryable across all surfaces (e.g. for future cross-source search, RSS feeds, entity extraction). Failure to write the bullets is non-fatal: the roundup page reads `intro_md`, not these mirror rows.\n\n**Backward compat.** Old single-paragraph roundups already in the DB still render correctly (no `###` headings → ReactMarkdown just renders a paragraph). To regenerate one to the new format: `POST /api/roundups/generate { topicId, date, lang }` (the cron is intentionally skip-if-exists).\n\n**Release.** Bump 2.3.1 → 2.4.0 via `npm run release:minor`. **Run migration 018 in Supabase before the next cron tick** — otherwise the `summary_bullets` insert silently warns and the roundup page is unaffected.",
    body_fr:
      "**Nouveau format de briefing.** Le roundup per-topic-jour à `/{topic}/r/{date}/{slug}` (rendu depuis `video_roundups.intro_md`) était jusqu'ici une intro mono-paragraphe de 3-5 phrases — trop maigre pour un produit éditorial. C'est désormais un **briefing structuré de 8 bullets**, chacun composé de :\n\n- Un **titre journalistique** (3-8 mots, ancré sur un nom propre, un produit ou un chiffre clé — ex: « GPT-5 passe à 2M tokens »)\n- Un **corps de 3-5 phrases** (cible 4) avec du **gras** sur les noms propres / chiffres / dates, écrit en style éditorial dense. Aucune référence aux vidéos / chaînes / speakers source — pure synthèse sujet-first.\n- 8 angles distincts, zéro redondance.\n\nStocké comme une séquence Markdown `### Titre\\n\\nbody...` dans la colonne `video_roundups.intro_md` existante (pas de migration sur le roundup lui-même), rendu avec un style custom `h3` (or, 17px, marges resserrées) sur la page SSR. Petit kicker « Briefing » ajouté au-dessus de la box pour signaler visuellement la nouvelle structure.\n\n**SEO resserré.** Le prompt interdit désormais les formulations génériques dans `seo_title` (« Briefing tech du jour »), impose 5 à 7 mots-clés spécifiques en kebab-case dans `slug_keywords` (mots interdits : `news`, `briefing`, `daily`, `ai`, `tech`, `video`, `today`), et exige que la `seo_description` contienne ≥ 3 termes spécifiques (noms de produits, chiffres, entreprises). Exemples concrets intégrés en EN + FR (« Sora 2 contre Veo 3 : OpenAI riposte avec un mode caméra natif » / « GPT-5 passe à 2M de contexte, Anthropic réplique avec Claude 4.6 »).\n\n**Le meilleur modèle dispo.** `gpt-4.1-mini` → **`gpt-5.3-chat-latest`** (le même modèle que `/api/news/top-summary` utilise déjà pour les résumés top 50). Le roundup est le flagship éditorial et le cron tourne en background function Netlify (budget 15 min), donc la latence est inutile à optimiser ; seule la qualité compte. `OPENAI_TIMEOUT_MS` passe 25s → **60s** pour laisser de la marge au modèle plus lent (mais plus intelligent).\n\n**Miroir summary_bullets.** La migration **018** ajoute `summary_bullets.video_roundup_id INT REFERENCES video_roundups(id) ON DELETE CASCADE` (+ index). À chaque écriture de roundup, les 8 bullets sont aussi persistés en 8 lignes dans `summary_bullets` avec `source_type = 'video_roundup'`, `text = '**Titre**\\n\\nBody'`, `bullet_index = 0..7`. C'est le même pattern déjà en place pour les `source_type` `article` / `video` / `top50` — les bullets deviennent queryables uniformément sur toutes les surfaces (futur search cross-source, flux RSS, extraction d'entités). Un échec d'écriture est non-fatal : la page roundup lit `intro_md`, pas ces lignes miroir.\n\n**Compat ascendante.** Les anciens roundups mono-paragraphe déjà en DB s'affichent toujours correctement (pas de `###` → ReactMarkdown rend juste un paragraphe). Pour régénérer un ancien roundup au nouveau format : `POST /api/roundups/generate { topicId, date, lang }` (le cron skip volontairement les buckets déjà faits).\n\n**Release.** Bump 2.3.1 → 2.4.0 via `npm run release:minor`. **Lancer la migration 018 dans Supabase avant le prochain tick du cron** — sinon l'insert `summary_bullets` warn silencieusement et la page roundup reste inchangée.",
    created_at: "2026-04-23T14:00:00Z",
  },
  {
    version: "2.3.1",
    title_en: "v2.3.1: Longer videos transcribe reliably (25s OpenAI budget, 3-tier sampling), EN copy fix",
    title_fr: "v2.3.1 : Vidéos longues transcrites de façon fiable (budget OpenAI 25s, sampling 3 tiers), fix copy EN",
    body_en:
      "**Transcribe — fewer 504s on long podcasts.** `/api/youtube-channels/transcribe` was hitting a 20s OpenAI timeout on 1h30+ videos and surfacing as `HTTP 504 Vidéo trop longue`. Two changes to use the full 30s Netlify budget:\n\n1. `OPENAI_TIMEOUT_MS` bumped 20s → **25s**, leaving ~5s for TranscriptAPI fetch + DB writes + SDK overhead.\n2. New 3-tier transcript sampling instead of 2 tiers:\n   - ≤ 50K chars: full transcript (≤ 30 min videos)\n   - 50K – 150K: 32K head + 14K tail = 46K chars (~11K tokens) — most podcasts up to ~2h30\n   - **> 150K: 28K head + 12K tail = 40K chars (~10K tokens)** — safety net for 3h+ podcasts\n\nThe aggressive threshold moved 120K → 150K and the aggressive sample grew 30K → 40K, both made possible by the longer GPT budget. The existing `[transcribe] truncated transcript ... (normal|aggressive)` log line now also includes the wordCount, making it easy to diagnose which tier a stuck video falls into in Netlify Function logs.\n\n**EN copy fix.** The menu pill label `videosBtn` was « Today's Video » (singular) — corrected to **« Today's Videos »** to match the FR « Vidéos du jour ».\n\n**Release.** Bump 2.3.0 → 2.3.1 via `npm run release:patch`.",
    body_fr:
      "**Transcribe — moins de 504 sur les podcasts longs.** `/api/youtube-channels/transcribe` tombait sur un timeout OpenAI de 20s pour les vidéos de 1h30+ et remontait en `HTTP 504 Vidéo trop longue`. Deux changements pour exploiter le budget 30s de Netlify :\n\n1. `OPENAI_TIMEOUT_MS` passe de 20s → **25s**, laissant ~5s pour le fetch TranscriptAPI + les writes DB + l'overhead SDK.\n2. Nouveau sampling de transcript en 3 tiers au lieu de 2 :\n   - ≤ 50K chars : transcript complet (vidéos ≤ 30 min)\n   - 50K – 150K : 32K head + 14K tail = 46K chars (~11K tokens) — la plupart des podcasts jusqu'à ~2h30\n   - **> 150K : 28K head + 12K tail = 40K chars (~10K tokens)** — filet de sécurité pour les podcasts 3h+\n\nLe seuil agressif passe 120K → 150K et l'échantillon agressif grandit 30K → 40K, les deux rendus possibles par le budget GPT plus large. Le log existant `[transcribe] truncated transcript ... (normal|aggressive)` inclut désormais aussi le wordCount, ce qui rend trivial de diagnostiquer dans quel tier tombe une vidéo qui plante dans les Netlify Function logs.\n\n**Fix copy EN.** Le pill menu `videosBtn` était « Today's Video » (singulier) — corrigé en **« Today's Videos »** pour matcher le FR « Vidéos du jour ».\n\n**Release.** Bump 2.3.0 → 2.3.1 via `npm run release:patch`.",
    created_at: "2026-04-23T10:00:00Z",
  },
  {
    version: "2.3",
    title_en: "v2.3: Recent transcribed videos list on Briefing, menu copy tweak, AI summary heading shortened",
    title_fr: "v2.3 : Liste des vidéos transcrites récentes sur le Briefing, copy menu, titre du résumé raccourci",
    body_en:
      "**Recent video pages on the Briefing.** New section at the bottom of the SPA homepage (`/app`) lists every transcribed YouTube video with an SSR page from the last 7 days, in the visitor's lang. Backed by a new `GET /api/video-pages/recent?days=7&lang=fr` endpoint (capped at 100 rows, edge-cached 60s + stale-while-revalidate 5min). Pulled from `video_transcriptions` joined on the new `slug_keywords + topic_id + published_date` triplet from v2.2. Rendered as a single card grouped by day (« ven. 25 avr. ») with each row showing a gold topic pill + the video title, all clickable into the SSR `/v/` page. Powers in-product discovery and pushes link juice to the `/v/` SEO pages from inside the SPA.\n\n**Menu copy.** The « Vidéos » / « Videos » pill becomes **« Vidéos du jour »** / **« Today's Video »** to make it clearer that this surface is the day-specific Videos page (not the SSR briefings hub).\n\n**SSR `/v/` heading.** « Résumé IA » / « AI summary » → **« Résumé »** / **« Summary »** on the per-video SSR page — shorter, cleaner, and the AI provenance is already obvious from the rest of the page.\n\n**Release.** Bump 2.2.0 → 2.3.0 via `npm run release:minor`.",
    body_fr:
      "**Vidéos récentes sur le Briefing.** Nouvelle section en bas de la home SPA (`/app`) qui liste chaque vidéo YouTube transcrite ayant une page SSR sur les 7 derniers jours, dans la langue du visiteur. Alimentée par un nouvel endpoint `GET /api/video-pages/recent?days=7&lang=fr` (capé à 100 rows, cache edge 60s + stale-while-revalidate 5min). Tiré de `video_transcriptions` joint sur le triplet `slug_keywords + topic_id + published_date` introduit en v2.2. Rendu en une card unique groupée par jour (« ven. 25 avr. »), chaque ligne avec un pill or topic + le titre vidéo, le tout cliquable vers la page SSR `/v/`. Booste la découverte in-product et redistribue du jus SEO vers les pages `/v/` depuis l'intérieur de la SPA.\n\n**Copy menu.** Le pill « Vidéos » / « Videos » devient **« Vidéos du jour »** / **« Today's Video »** pour clarifier que cette surface est la page Vidéos jour-par-jour (pas le hub des briefings SSR).\n\n**Titre `/v/`.** « Résumé IA » / « AI summary » → **« Résumé »** / **« Summary »** sur la page per-vidéo SSR — plus court, plus propre, et la provenance IA est déjà évidente depuis le reste de la page.\n\n**Release.** Bump 2.2.0 → 2.3.0 via `npm run release:minor`.",
    created_at: "2026-04-22T18:00:00Z",
  },
  {
    version: "2.2",
    title_en: "v2.2: SSR video pages (/v/), per-topic roundups (/r/), /briefings hub, dynamic landing console, top-story API",
    title_fr: "v2.2 : Pages SSR vidéo (/v/), briefings per-topic (/r/), hub /briefings, console landing dynamique, API top-story",
    body_en:
      "**Per-video SSR pages (/v/).** Every transcribed YouTube video now has its own SEO-optimized page at `/{topic}/v/{YYYY-MM-DD}/{slug}`. Migration 016 adds `slug_keywords` + `published_date` columns on `video_transcriptions` with a unique route index; a deterministic `slugifyVideoTitle()` + DB-aware `uniquifyVideoSlug()` helper pair produce the 4-5-keyword slugs at transcribe time; `scripts/backfill-video-slugs.mjs` (idempotent, auto-loads `.env`) also backfills `topic_id` from `youtube_channels` for legacy rows. The page itself renders the YouTube embed, a TTS audio player (`VideoPageAudio` Client Component reading cookie-backed speed/voice with sensible defaults), the AI summary via `react-markdown`, the full transcript in a collapsible `<details>` (keyword-rich for crawlers), a \"Sur le même sujet\" sidebar of 3 recent videos in the same topic, JSON-LD `VideoObject` + `Article`, OG `video.other`, canonical strict-to-self, and hreflang to the alt-lang slug via `getVideoPageAltLang`. Same-session convention: the slug is per-lang, so `/v/{date}/{slug-en}` and `/v/{date}/{slug-fr}` coexist and the URL alone determines the lang — no dual-lookup fallback needed.\n\n**Per-topic-per-day roundups (/r/).** New `video_roundups` table (migration 017). For each `(topic, date, lang)` bucket with at least 2 transcribed videos, `src/lib/generate-video-roundup.ts` asks gpt-4.1-mini to emit a strict JSON blob (seo_title ≤ 70 chars, seo_description ≤ 160 chars, kebab-case slug, 3-5 sentence intro_md) and upserts it. Two call sites: owner-only `POST /api/roundups/generate` for manual triggers, and a Netlify background function `cron-video-roundup-background.ts` modeled on the daily-summary cron (resumable via bulk SELECT of already-done buckets, MAX_TOPICS_PER_RUN=5, deterministic id-sort). The SSR route `/{topic}/r/{date}/{slug}` renders the AI intro, each constituent video as a card (3 bullets + link to `/v/`), JSON-LD `CollectionPage` + `ItemList`, hreflang, and a \"previous roundups\" block.\n\n**`/briefings` public hub.** Replaces the `/video-briefings` placeholder with a permanent URL (intentionally generic — no \"video\" in the path, forward-compatible for future briefing types). Lists every roundup chronologically (grouped by day) plus a per-topic grid mirroring `/summaries`. Bilingual via `?lang=fr|en`.\n\n**New menu item.** `GeneralMenu` (SPA + SSR) gains a \"Video briefings / Briefings vidéo\" pill between \"Videos\" and \"Articles\" pointing at `/briefings`. New i18n key `videoBriefingsBtn`. SPA pill is a hard `<a>` (full navigation — the SPA can't render the SSR page in place). `SeoGeneralMenu` accepts `activePage=\"videoBriefings\"`.\n\n**Topic slug guard.** New helper `isReservedTopicSlug()` in `src/lib/topics.ts` blocks `v`, `r`, `briefings`, `summaries`, `app`, `api`, `videos`, `top-articles`, `favorites` from being used as topic IDs — wired into `POST /api/topics` to prevent a future topic from shadowing a fixed route segment.\n\n**Topic hub video coverage.** The existing `/{topic}` hub (page 1 only) gains a \"Couverture vidéo / Video coverage\" section listing the 3 most recent roundups + 5 most recent per-video pages of the topic, with an anchor `#video-coverage` and a \"Tous les briefings →\" link.\n\n**Sitemap expanded.** `sitemap.xml` now includes `/briefings`, every `/v/` URL from the last 90 days (new `getAllVideoPageRoutes()` helper) and every `/r/` URL from the last 90 days (new `getAllVideoRoundupRoutes()`). Same 90-day retroactive cap applied to `getAllSummaryRoutes()` for consistency (`SITEMAP_RECENT_DAYS` constant). ~742 entries today, scales linearly with transcribed content.\n\n**VideoCard read-article link.** Cards in BriefingPage + VideosPage now show a gold \"Lire l'article / Read article →\" in the meta row when the video has a slug — points at `/{topic}/v/{date}/{slug}`. The `/api/youtube-channels/videos` response now carries `topicId` + `slugKeywords` + `publishedDate` per row, consumers pick them up automatically through the `{ summaryMd, ...rest }` destructure pattern already in use.\n\n**Dynamic landing console.** Replaces the static 6-row mock on `LandingConsole` with real data: pulls articles scored 10/9/8/7/5/3 (one each, distinct topics) from the last 24h via an async Server Component, plus the exact 24h article count for the footer (\"avg delay 1m25s\" kept hardcoded per request). Falls back gracefully to the mock if Supabase is unavailable or too few rows match.\n\n**Briefing top-story.** New `GET /api/news/top-story?lang=` endpoint returns one freshly fetched 10/10 article via a ladder query (score=10 in 1h → ≥9 in 1h → score=10 in 24h → ≥9 in 24h). The BriefingPage hero now prefers this dedicated query over `topFeed[0]` and deduplicates the hero from the Top 5 below.\n\n**Release.** Bump 2.1.0 → 2.2.0 via `npm run release:minor`. Migrations 016 + 017 must be run manually in Supabase, followed by `node scripts/backfill-video-slugs.mjs` (idempotent).",
    body_fr:
      "**Pages SSR per-vidéo (/v/).** Chaque vidéo YouTube transcrite a désormais sa propre page SEO à `/{topic}/v/{YYYY-MM-DD}/{slug}`. La migration 016 ajoute les colonnes `slug_keywords` + `published_date` sur `video_transcriptions` avec un index unique de route ; une paire de helpers `slugifyVideoTitle()` (déterministe) + `uniquifyVideoSlug()` (DB-aware) produit les slugs de 4-5 mots-clés à l'écriture ; `scripts/backfill-video-slugs.mjs` (idempotent, charge automatiquement `.env`) backfille aussi le `topic_id` depuis `youtube_channels` pour les anciennes lignes. La page affiche l'embed YouTube, un lecteur TTS (`VideoPageAudio` Client Component qui lit les cookies speed/voice avec fallback par défaut), le résumé IA via `react-markdown`, la transcription complète dans un `<details>` repliable (riche en mots-clés pour les crawlers), une sidebar « Sur le même sujet » avec 3 vidéos récentes du même topic, JSON-LD `VideoObject` + `Article`, OG `video.other`, canonical strict vers self, et hreflang vers le slug dans l'autre langue via `getVideoPageAltLang`. Convention : le slug est per-lang, donc `/v/{date}/{slug-en}` et `/v/{date}/{slug-fr}` coexistent et l'URL seule détermine la langue — pas de dual-lookup.\n\n**Roundups per-topic-jour (/r/).** Nouvelle table `video_roundups` (migration 017). Pour chaque bucket `(topic, date, lang)` avec au moins 2 vidéos transcrites, `src/lib/generate-video-roundup.ts` demande à gpt-4.1-mini un JSON strict (seo_title ≤ 70 char, seo_description ≤ 160 char, slug kebab-case, intro_md de 3-5 phrases) et l'upsert. Deux points d'entrée : `POST /api/roundups/generate` owner-only pour déclencher manuellement, et une fonction background Netlify `cron-video-roundup-background.ts` modelée sur le cron daily-summary (resumable via bulk SELECT des buckets déjà faits, MAX_TOPICS_PER_RUN=5, tri id déterministe). La route SSR `/{topic}/r/{date}/{slug}` rend l'intro IA, chaque vidéo en card (3 bullets + lien vers `/v/`), JSON-LD `CollectionPage` + `ItemList`, hreflang, et un bloc « Briefings précédents ».\n\n**Hub public `/briefings`.** Remplace le placeholder `/video-briefings` par une URL définitive (volontairement générique — pas de « video » dans le path, forward-compatible pour d'autres types de briefings). Liste tous les roundups chronologiquement (groupés par jour) plus une grille par topic comme `/summaries`. Bilingue via `?lang=fr|en`.\n\n**Nouvel item menu.** `GeneralMenu` (SPA + SSR) gagne un pill « Briefings vidéo » entre « Vidéos » et « Articles » pointant vers `/briefings`. Nouvelle clé i18n `videoBriefingsBtn`. Le pill SPA est un `<a>` hard (navigation complète — la SPA ne peut pas rendre la page SSR en place). `SeoGeneralMenu` accepte `activePage=\"videoBriefings\"`.\n\n**Garde des slugs de topic.** Nouveau helper `isReservedTopicSlug()` dans `src/lib/topics.ts` bloque `v`, `r`, `briefings`, `summaries`, `app`, `api`, `videos`, `top-articles`, `favorites` comme IDs de topic — branché sur `POST /api/topics` pour éviter qu'un futur topic ne shadow un segment de route fixe.\n\n**Couverture vidéo sur le topic hub.** La page `/{topic}` existante (page 1 uniquement) gagne une section « Couverture vidéo » listant les 3 derniers roundups + 5 dernières pages per-vidéo du topic, avec un anchor `#video-coverage` et un lien « Tous les briefings → ».\n\n**Sitemap enrichi.** `sitemap.xml` inclut maintenant `/briefings`, chaque URL `/v/` sur les 90 derniers jours (nouveau helper `getAllVideoPageRoutes()`) et chaque URL `/r/` sur les 90 derniers jours (nouveau `getAllVideoRoundupRoutes()`). Le même cap 90 jours est appliqué rétroactivement à `getAllSummaryRoutes()` pour cohérence (constante `SITEMAP_RECENT_DAYS`). ~742 entries aujourd'hui, scale linéairement.\n\n**Lien « Lire l'article » sur VideoCard.** Les cards dans BriefingPage + VideosPage affichent désormais un lien doré « Lire l'article / Read article → » dans la ligne meta quand la vidéo a un slug — pointe vers `/{topic}/v/{date}/{slug}`. La réponse `/api/youtube-channels/videos` renvoie `topicId` + `slugKeywords` + `publishedDate` par ligne, les consumers les propagent automatiquement via le pattern `{ summaryMd, ...rest }` déjà en place.\n\n**Console landing dynamique.** Remplace les 6 lignes mock statiques de `LandingConsole` par de vraies données : sélectionne des articles scorés 10/9/8/7/5/3 (un de chaque, topics distincts) depuis les 24h via un Server Component async, plus le count réel des articles collectés sur 24h pour le footer (« délai moyen 1m25s » gardé en dur comme demandé). Fallback propre vers le mock si Supabase est indisponible ou si trop peu de lignes matchent.\n\n**Top story Briefing.** Nouvel endpoint `GET /api/news/top-story?lang=` qui renvoie un article 10/10 fraîchement collecté via une search ladder (score=10 en 1h → ≥9 en 1h → score=10 en 24h → ≥9 en 24h). Le hero de la BriefingPage préfère maintenant cette requête dédiée sur `topFeed[0]` et dédupique le hero du Top 5 en dessous.\n\n**Release.** Bump 2.1.0 → 2.2.0 via `npm run release:minor`. Migrations 016 + 017 à exécuter manuellement dans Supabase, suivies de `node scripts/backfill-video-slugs.mjs` (idempotent).",
    created_at: "2026-04-22T12:00:00Z",
  },
  {
    version: "2.1",
    title_en: "v2.1: Briefing loading skeletons, opt-in Top-50 AI summary, responsive landing nav polish, cache TTL tuned",
    title_fr: "v2.1 : Skeletons de chargement Briefing, résumé IA Top 50 en opt-in, refonte responsive du nav landing, TTL de cache ajusté",
    body_en:
      "**Briefing — no more flash of empty content.** While the \"Vidéos transcrites\" and \"Résumé du jour\" fetches are in flight, the page now renders a discrete `SectionSpinner` placeholder (localised kicker + centered spinner + reserved `marginBottom: 36`) in the same position the real section will occupy. When the API resolves, the real content replaces the placeholder in place with no layout jump. `aria-busy=\"true\"` + `aria-live=\"polite\"` for screen readers. Both video + summary loading states are cleanly closed in a `.finally()` so a failed fetch still hides the spinner (instead of being stuck spinning).\n\n**Top 50 — opt-in AI summary, no more wasted tokens.** Landing on `/app/top-articles` used to fire an OpenAI call on every visit, even when the user just wanted to browse the 50 articles. The page now renders a single gold-bordered card with a clear CTA: *\"Generate AI summary / Générer le résumé IA\"*, plus a short hint explaining what they'll get. Clicking the CTA drives the card through three states — `idle → loading → ready` — computed from one derived variable so no render can ever land on \"all branches false\" (that was the source of the 1-frame flicker on click). Dedup key is preserved: reopening the page with the same top-50 list re-uses the cached summary instead of calling OpenAI again. `SummaryBox` gains an `embedded` prop so it integrates cleanly inside the gold card without double-chrome. Smooth crossfade via a new `topSummaryFadeIn` keyframe animation in `globals.css`. Three new i18n keys (`topSummaryCtaTitle`, `topSummaryCtaHint`, `topSummaryCtaButton`).\n\n**Landing nav — responsive, polished.** ~127 lines of CSS added to the landing nav: explicit responsive strategy (≥ 960px full layout, < 960px hides the center anchor links — they stay reachable via page scroll — , < 640px also hides the \"Sign in\" link to keep only logo + lang switch + primary CTA visible on small screens); nav-bar height reduced from 96 px to 80 px for more balanced proportions with the 40 px logo image; subtle gold underline that grows under each center link on hover; opacity fade on logo hover; `transform: translateY(1px)` on `.btn-primary:active` for tactile feedback; keyboard `:focus-visible` ring; `-webkit-backdrop-filter` prefix so the sticky blur works on Safari. `white-space: nowrap` + `min-width: 0` combo stops \"Vidéo IA\" from wrapping onto two lines at cramped widths.\n\n**Copy & menu polish.** *\"Today's Top\"* / *\"Top du jour\"* becomes **\"Today's Top 50\"** / **\"Top 50 du jour\"** (and the homepage Top 20 subtitle likewise mentions *Top 50*) — clearer number anchor for the value proposition. `GeneralMenu` pills get `padding: 6px 14px`, `min-width: 72px`, inline-flex centering so the shortest labels (Vidéos, Topics) stay visually balanced next to the longer ones without forcing a uniform pill width.\n\n**Cache TTL tuned.** AI news summaries (`/api/news`) re-compute more aggressively: ≤ 1 h stays at 5 min, ≤ 6 h drops from 15 → **10 min**, ≤ 24 h drops from 30 → **10 min**, > 24 h drops from 60 → **30 min**. Rationale: a shorter window means fresher data is more valuable, so we re-spend OpenAI tokens sooner. `SPEC.md` table updated accordingly. `getCacheTtlMinutes()` gains a JSDoc block explaining the trade-off.\n\n**Release.** Bump 2.0.0 → 2.1.0 via `npm run release:minor`. Display form is the compact `v2.1` (trailing `.0` stripped).",
    body_fr:
      "**Briefing — plus de flash de contenu vide.** Pendant que les fetches « Vidéos transcrites » et « Résumé du jour » sont en vol, la page rend désormais un placeholder discret `SectionSpinner` (kicker localisé + spinner centré + `marginBottom: 36` réservé) à l'emplacement exact où la vraie section se placera. Quand l'API résout, le vrai contenu remplace le placeholder sur place sans saut de layout. `aria-busy=\"true\"` + `aria-live=\"polite\"` pour les lecteurs d'écran. Les deux loading states (vidéos + résumé) sont proprement fermés dans un `.finally()` donc un fetch qui échoue masque aussi le spinner (au lieu de rester bloqué en tournant).\n\n**Top 50 — résumé IA en opt-in, plus de tokens gaspillés.** Arriver sur `/app/top-articles` déclenchait jusqu'ici un appel OpenAI à chaque visite, même quand l'utilisateur voulait juste parcourir les 50 articles. La page rend désormais une carte unique à bordure or avec un CTA clair : *« Générer le résumé IA / Generate AI summary »*, plus un court hint expliquant ce qu'il va obtenir. Le clic sur le CTA fait passer la carte par trois états — `idle → loading → ready` — calculés depuis une seule variable dérivée, donc aucun rendu ne peut atterrir sur « toutes branches fausses » (c'était la source du flash 1-frame au clic). La clé de déduplication est conservée : rouvrir la page avec le même top 50 réutilise le résumé en cache au lieu de relancer OpenAI. `SummaryBox` gagne un prop `embedded` pour s'intégrer proprement dans la carte or sans double-chrome. Crossfade fluide via un nouveau `@keyframes topSummaryFadeIn` dans `globals.css`. Trois nouvelles clés i18n (`topSummaryCtaTitle`, `topSummaryCtaHint`, `topSummaryCtaButton`).\n\n**Landing nav — responsive, polishé.** ~127 lignes de CSS ajoutées au nav landing : stratégie responsive explicite (≥ 960 px layout complet, < 960 px masque les liens d'ancre centraux — ils restent accessibles en scrollant la page — , < 640 px masque aussi le lien « Sign in » pour ne garder que logo + lang switch + CTA primaire sur les petits écrans) ; hauteur du nav-bar ramenée de 96 à 80 px pour des proportions plus équilibrées avec l'image logo de 40 px ; underline doré subtil qui grandit sous chaque lien central au hover ; fade d'opacité sur le hover du logo ; `transform: translateY(1px)` sur `.btn-primary:active` pour un retour tactile ; keyboard ring `:focus-visible` ; préfixe `-webkit-backdrop-filter` pour que le blur sticky fonctionne sur Safari. Le combo `white-space: nowrap` + `min-width: 0` empêche « Vidéo IA » de wrapper sur deux lignes aux largeurs serrées.\n\n**Polish copy & menu.** *« Today's Top »* / *« Top du jour »* devient **« Today's Top 50 »** / **« Top 50 du jour »** (et le sous-titre Top 20 de la home mentionne aussi *Top 50*) — ancrage chiffré plus clair pour la value proposition. Les pills du `GeneralMenu` reçoivent `padding: 6px 14px`, `min-width: 72px`, centrage inline-flex pour que les libellés les plus courts (Vidéos, Topics) restent visuellement équilibrés à côté des plus longs sans forcer une largeur uniforme.\n\n**TTL de cache ajusté.** Les résumés IA de news (`/api/news`) se recalculent plus agressivement : ≤ 1 h reste à 5 min, ≤ 6 h passe de 15 → **10 min**, ≤ 24 h passe de 30 → **10 min**, > 24 h passe de 60 → **30 min**. Rationale : une fenêtre plus courte signifie que la fraîcheur de la donnée est plus précieuse, donc on re-dépense les tokens OpenAI plus tôt. La table `SPEC.md` est mise à jour. `getCacheTtlMinutes()` gagne un bloc JSDoc qui explique le trade-off.\n\n**Release.** Bump 2.0.0 → 2.1.0 via `npm run release:minor`. Forme d'affichage compacte `v2.1` (le `.0` final est strippé).",
    created_at: "2026-04-21T16:00:00Z",
  },
  {
    version: "2.0",
    title_en: "v2.0: New Briefing homepage, trending topics API, VideoCard refactor, timezone-aware video listings, resumable daily-summary cron",
    title_fr: "v2.0 : Nouvelle homepage Briefing, API tendances, refactor VideoCard, listing vidéos timezone-aware, cron daily-summary resumable",
    body_en:
      "**Briefing — the new SPA homepage.** A brand-new `BriefingPage` becomes the default landing inside `/app`, replacing the Videos page in that role (Videos moves to `/app/videos`). The page brings the day's signal into one place: top scored articles via the existing top-feed hook with the unified `ScoreMeter`, the latest YouTube videos rendered through a shared `VideoCard`, a real-time \"trending topics\" strip backed by a new API, and links into the deeper SPA pages (Top 50, Daily Summaries, Favorites). New `briefing` value added to `AppNavPage`; `PAGE_PATHS` maps `/app` → Briefing and `/app/briefing`, `/app/videos`, `/app/articles` resolve cleanly; `pathToPage` treats `/`, `/app` and `/app/briefing` as the briefing route so a hard refresh on any of them lands on the same page. Header home icon, GeneralMenu (a new « Briefing » pill is leftmost), AuthModal post-signup welcome screen and every auth-fallback redirect now point at Briefing instead of Videos.\n\n**Trending topics API.** `GET /api/topics/trending?since=6h&lang=fr` returns the top 8 topics by article count in the recent window, with `{ id, label, count }`. Window parsing accepts `Nh` / `Nd`; results capped to active and displayed topics; bounded memory (5 K-row pull, in-memory tally). Cached for 60 s on the client and 5 min on the edge. Powers the « Tendances » strip on Briefing.\n\n**Shared `VideoCard` component.** ~520 lines of video-card markup are extracted from `VideosPage` into a standalone `src/app/components/VideoCard.tsx` (with a typed `VideoItem` export). The card carries everything that was already there — embed playback toggle, summary auto-expand, transcript / .md download popover, copy-link / favorite / open-on-YouTube icons, audio player, error fallback. `VideosPage` now consumes it (the file shrinks from ~910 to ~390 lines) and `BriefingPage` uses the same component, so any future video-card improvement lands in both surfaces at once.\n\n**Timezone-aware video listings.** `GET /api/youtube-channels/videos` now accepts `?tz=<IANA>` (e.g. `Europe/Paris`). When present, the route resolves the requested local calendar day into precise UTC `[start, end)` bounds via `Intl.DateTimeFormat` longOffset and filters on the `published` timestamptz column instead of the legacy `published_date` (computed in UTC). This fixes the off-by-one-day bug where a video published 00h–02h Paris time was filed under the previous UTC day. Without `?tz` the legacy filter is preserved (backwards compatible).\n\n**Resumable daily-summary cron.** `cron-daily-summary-background.ts` gains a `MAX_TOPICS_PER_RUN` cap (default 5, env-overridable) so a single tick never overruns Netlify's 15-min wall, plus a bulk SELECT of existing `(topic, lang)` rows that lets the loop fast-skip already-generated summaries. Topics are processed in deterministic id order so consecutive cron ticks naturally pick up where the previous left off — full coverage of all topics is now spread across multiple ticks instead of being all-or-nothing per run.\n\n**Bump rationale.** Bumped to 2.0 (rather than 1.111) because the SPA's default landing changes — every existing bookmark on `/app` now lands on a different page. Strictly speaking this is a user-visible behavioural change at the route level. APP_VERSION + version.json + landing pricing copy + landing footer line all bumped to 2.0 via `npm run release:major`. Display form is the compact `v2.0` (trailing `.0` stripped); `package.json` carries the full semver `2.0.0`.",
    body_fr:
      "**Briefing — la nouvelle homepage SPA.** Un tout nouveau `BriefingPage` devient le landing par défaut dans `/app`, en remplacement de la page Vidéos dans ce rôle (Vidéos passe sur `/app/videos`). La page rassemble le signal du jour en un seul endroit : top articles scorés via le hook top-feed existant avec le `ScoreMeter` unifié, dernières vidéos YouTube rendues via un `VideoCard` partagé, strip « Tendances » temps-réel alimenté par une nouvelle API, et liens vers les pages SPA en profondeur (Top 50, Résumés quotidiens, Favoris). Nouvelle valeur `briefing` ajoutée à `AppNavPage` ; `PAGE_PATHS` mappe `/app` → Briefing et `/app/briefing`, `/app/videos`, `/app/articles` résolvent proprement ; `pathToPage` traite `/`, `/app` et `/app/briefing` comme la route briefing, donc un hard refresh sur n'importe laquelle atterrit sur la même page. Icône maison du header, GeneralMenu (nouveau pill « Briefing » en première position), écran d'accueil post-signup d'AuthModal et chaque redirection auth-fallback pointent désormais vers Briefing au lieu de Vidéos.\n\n**API tendances.** `GET /api/topics/trending?since=6h&lang=fr` renvoie les 8 topics du moment par nombre d'articles dans la fenêtre récente, avec `{ id, label, count }`. Parsing de fenêtre accepte `Nh` / `Nd` ; résultats limités aux topics actifs et affichés ; mémoire bornée (pull de 5 K rows, comptage en mémoire). Cache 60 s côté client et 5 min côté edge. Alimente le strip « Tendances » sur Briefing.\n\n**Composant `VideoCard` partagé.** ~520 lignes de markup video-card sont extraites de `VideosPage` dans un composant autonome `src/app/components/VideoCard.tsx` (avec un export typé `VideoItem`). La card embarque tout ce qui y était déjà — toggle de lecture embed, auto-déploiement du résumé, pop-up de téléchargement transcript / .md, icônes copy-link / favori / ouvrir sur YouTube, lecteur audio, fallback d'erreur. `VideosPage` la consomme désormais (le fichier passe de ~910 à ~390 lignes) et `BriefingPage` utilise le même composant, donc toute évolution future de la card vidéo atterrit dans les deux surfaces à la fois.\n\n**Listing vidéos timezone-aware.** `GET /api/youtube-channels/videos` accepte maintenant `?tz=<IANA>` (ex. `Europe/Paris`). Quand présent, la route résout le jour calendaire local demandé en bornes UTC `[start, end)` précises via `Intl.DateTimeFormat` longOffset et filtre sur la colonne `published` timestamptz au lieu de l'ancien `published_date` (calculé en UTC). Corrige le bug off-by-one-day où une vidéo publiée 00h–02h heure de Paris se retrouvait classée sur le jour UTC précédent. Sans `?tz`, le filtre historique est préservé (rétrocompatible).\n\n**Cron daily-summary resumable.** `cron-daily-summary-background.ts` gagne un cap `MAX_TOPICS_PER_RUN` (défaut 5, surchargeable par env) pour qu'un tick unique ne dépasse jamais le mur Netlify 15 min, plus un bulk SELECT des `(topic, lang)` déjà présents qui permet à la boucle de fast-skiper les résumés déjà générés. Les topics sont traités dans un ordre déterministe par id, donc des ticks cron consécutifs reprennent naturellement là où le précédent s'est arrêté — la couverture complète de tous les topics se répartit maintenant sur plusieurs ticks au lieu d'être all-or-nothing par run.\n\n**Justification du bump.** Bumpé en 2.0 (plutôt que 1.111) car le landing par défaut de la SPA change — chaque bookmark existant sur `/app` atterrit maintenant sur une page différente. Strictement parlant, c'est un changement de comportement visible utilisateur au niveau route. APP_VERSION + version.json + copy pricing landing + ligne du footer landing tous bumpés à 2.0 via `npm run release:major`. La forme d'affichage est la version compacte `v2.0` (le `.0` final est strippé) ; `package.json` porte le semver complet `2.0.0`.",
    created_at: "2026-04-21T08:00:00Z",
  },
  {
    version: "1.110",
    title_en: "v1.110: Unified ScoreMeter across the product, signup welcome screen, branding tightened",
    title_fr: "v1.110 : ScoreMeter unifié dans tout le produit, écran d'accueil post-inscription, branding resserré",
    body_en:
      "**ScoreMeter — one component, every score.** New `src/app/components/ScoreMeter.tsx`: a small mono number stacked just above a thin progress bar, the two reading as one indicator, with tier coloring (green ≥ 9, gold ≥ 5, orange ≥ 3, red < 3). It replaces every ad-hoc `9/10` span across the product so the score reads identically everywhere. Migrated consumers: the SPA `ArticleCard` (homepage Top feed), `AllArticlesTab`, `TopFeedSection`, `DailySummaryArticles`, the SSR daily summary page (`/{topic}/{date}/{slug}`), and the landing's hero scoring console (which dropped its old square `.score-badge` cascade entirely; the grid moved from 3 to 2 columns and a new `.score-meter` / `.score-num` rule pair drives the layout). The new component is standalone — it lives outside the landing CSS scope so the SPA doesn't depend on `landing.css`.\n\n**API surface.** `/api/news` now exposes `score` per item alongside the existing fields; `ArticleSummary` in `src/lib/types.ts` gains `score?: number | null`. The daily-summary generator (`src/lib/generate-daily-summary.ts`) preserves the per-article `relevance_score` in the persisted JSON of `daily_summaries.articles`, so the SEO-rendered page can show the meter for previously generated summaries too. Backwards compatible: consumers that don't carry the field through still render fine.\n\n**Signup welcome screen.** `AuthModal` swaps to a welcome state right after a successful signup (Supabase email confirmation is disabled, so the session is live immediately): a short \"Welcome to 8news / Bienvenue sur 8news\" message explains that the team is iterating fast and that user feedback matters, with a single Close CTA. Closing the welcome screen does a hard navigation to `/app/videos` so the SPA picks up the fresh Supabase session via middleware regardless of where the modal was triggered (landing, SPA, SSR page). Three new i18n keys (`authWelcomeTitle`, `authWelcomeBody`, `authWelcomeClose`).\n\n**Branding tightened.** The global subtitle (header + footer everywhere) becomes **Tech • IA • Crypto** (middle dot, bilingual), replacing the old `Tech * AI * Crypto`. The landing hero kicker switches from `Tech / AI / Crypto` to `Tech · AI · Crypto` and is now styled big enough to fill the hero copy column at wide breakpoints (`clamp(17px, 3.9vw, 39px)`, full-width, `white-space: nowrap`). The landing hero sub is reworded to lead with the YouTube channels and headline article volume (400+ EN / 5000 FR per day) over RSS feed counts. The English `summary` label drops the redundant \"AI\" prefix; the French stays `Résumé IA`. Historical changelog entries (v1.106, v1.107) are updated in-place to use the new `Tech • IA • Crypto` form so the in-product changelog stays self-consistent (the API filter overrides title/body from code, no DB migration needed).\n\n**Tooling.** `scripts/release.mjs` now ends with two advisory checks (introduced in 1.109.x but documented here): a ✓/⚠ on whether the current version has an entry in `CHANGELOG_ENTRIES`, plus a ⚠ list of commits since the last `chore(release):` commit so parallel-session commits don't slip through unmentioned. Both are observation-only; the script never fails the release.\n\n**Release.** Bump 1.109.2 → 1.110.0 via `npm run release:minor`. Display form is the compact `v1.110` (trailing `.0` stripped).",
    body_fr:
      "**ScoreMeter — un composant, tous les scores.** Nouveau `src/app/components/ScoreMeter.tsx` : un petit chiffre mono empilé juste au-dessus d'une barre de progression fine, l'ensemble se lisant comme un indicateur unique, avec coloration par palier (vert ≥ 9, or ≥ 5, orange ≥ 3, rouge < 3). Il remplace chaque affichage ad hoc `9/10` dans le produit pour que le score se lise de la même façon partout. Consumers migrés : `ArticleCard` SPA (Top du jour), `AllArticlesTab`, `TopFeedSection`, `DailySummaryArticles`, la page SSR daily summary (`/{topic}/{date}/{slug}`), et la console de scoring du hero landing (qui a abandonné son ancienne cascade carrée `.score-badge` ; la grid passe de 3 à 2 colonnes et un nouveau couple de règles `.score-meter` / `.score-num` pilote le layout). Le nouveau composant est autonome — il vit hors du scope CSS landing, donc la SPA n'a pas de dépendance à `landing.css`.\n\n**API surface.** `/api/news` expose désormais `score` par item à côté des champs existants ; `ArticleSummary` dans `src/lib/types.ts` gagne `score?: number | null`. Le générateur de daily summary (`src/lib/generate-daily-summary.ts`) préserve le `relevance_score` par article dans le JSON persisté de `daily_summaries.articles`, pour que la page SEO rendue puisse afficher le meter même sur des résumés générés auparavant. Rétrocompatible : les consumers qui ne transportent pas le champ continuent de rendre correctement.\n\n**Écran d'accueil post-inscription.** `AuthModal` swap vers un état welcome juste après un signup réussi (la confirmation email Supabase étant désactivée, la session est live immédiatement) : un court message « Welcome to 8news / Bienvenue sur 8news » explique que l'équipe itère rapidement et que les retours utilisateurs comptent, avec un unique CTA Close. La fermeture de l'écran welcome fait une navigation hard vers `/app/videos` pour que la SPA prenne en compte la nouvelle session Supabase via le middleware, peu importe d'où la modal a été déclenchée (landing, SPA, page SSR). Trois nouvelles clés i18n (`authWelcomeTitle`, `authWelcomeBody`, `authWelcomeClose`).\n\n**Branding resserré.** Le subtitle global (header + footer partout) devient **Tech • IA • Crypto** (point médian, bilingue), en remplacement de l'ancien `Tech * AI * Crypto`. Le kicker du hero landing passe de `Tech / AI / Crypto` à `Tech · AI · Crypto` et est désormais stylé assez grand pour remplir la colonne copy du hero aux larges breakpoints (`clamp(17px, 3.9vw, 39px)`, full-width, `white-space: nowrap`). La sub du hero landing est reformulée pour mettre en avant les chaînes YouTube et le volume d'articles à la une (400+ EN / 5000 FR par jour) plutôt que le nombre de flux RSS. Le label `summary` EN abandonne le préfixe redondant « AI » ; le FR reste `Résumé IA`. Les entrées changelog historiques (v1.106, v1.107) sont mises à jour in-place pour utiliser la nouvelle forme `Tech • IA • Crypto` afin que le changelog in-product reste cohérent (le filtre API réécrit titre/body depuis le code, pas de migration DB nécessaire).\n\n**Tooling.** `scripts/release.mjs` se termine maintenant par deux checks advisory (introduits en 1.109.x mais documentés ici) : un ✓/⚠ pour savoir si la version courante a une entrée dans `CHANGELOG_ENTRIES`, plus une liste ⚠ des commits depuis le dernier commit `chore(release):` pour que les commits venant de sessions parallèles ne passent pas à la trappe. Les deux sont purement observationnels ; le script ne fait jamais échouer la release.\n\n**Release.** Bump 1.109.2 → 1.110.0 via `npm run release:minor`. La forme d'affichage est la version compacte `v1.110` (le `.0` final est strippé).",
    created_at: "2026-04-20T18:00:00Z",
  },
  {
    version: "1.109.2",
    title_en: "v1.109.2: YT section screenshot + reorder, transcribe stability, topics-on-mount fix, markdown fence cleanup",
    title_fr: "v1.109.2 : Section YT avec screenshot + réordonnée, stabilité de la transcription, fix topics au mount, nettoyage des fences Markdown",
    body_en:
      "**Landing.** The YouTube intelligence section is restructured into a two-column block (kicker + title + descriptive paragraph on the left, an actual product screenshot of an 8news YT summary on the right) and moves above the How-it-works section (Hero → Ticker → Stats → **YT** → How → Topics → Pricing → FAQ → CTA → Footer). The screenshot lives in `public/landing/yt-summary-preview.png` (`loading=\"lazy\"`); the section stacks vertically below 960px. Hero sub now leads with the YouTube channels and mentions 400+ RSS feeds second; the YT sub mentions the smart-bilingual flow (under 10s EN→FR translation) and the new one-click download of the full transcript (.txt) or summary (.md). `#features` gets a `border-top`. Root `<head>` description fixed (`400++` → `400+`) and reordered to match the hero copy.\n\n**Transcription stability.** The transcribe route was systematically tipping over Netlify's ~30 s synchronous-function budget on long podcasts (60 s+ in local dev), surfacing as opaque 502s in prod. Three targeted fixes: (1) per-request OpenAI timeout (`OPENAI_TIMEOUT_MS = 20s`) via the SDK's `timeout` option so a model call can never hang the function; (2) very long transcripts (> 50 K chars) sampled to head 32 K + tail 14 K with a `[…]` elision marker — typically the most signal-rich parts, full transcript preserved up to ~30 min, only 1 h+ ones get cropped, each truncation logged with `videoId`; (3) `compressSummaryIfNeeded` retry loop bypassed (kept in the file for future reuse) since each retry was another 10–20 s OpenAI call pushing past the 30 s wall — the prompt already enforces `SUMMARY_MAX_CHARS`, an oversized result is acceptable, a 502 is not. Plus operational polish: `console.error` every catch with `videoId` + `lang` + raw message; common failure patterns mapped to friendlier statuses (timeout/aborted → 504 + « Vidéo trop longue… », transcript 404 → 404 + « Pas de sous-titres exploitables », rate limit / 429 → 429 + « Limite atteinte… »); response now carries both `error` (user string) and `raw` (debug).\n\n**Markdown fence cleanup.** GPT sometimes wraps a long Markdown completion in a fenced code block (``` ```markdown\\n…\\n``` ```), which `react-markdown` then renders as a single monospace `<pre><code>` with the raw `## INTRO` / `**bold**` syntax visible. New `stripCodeFences()` helper applied at both ends: at write time in `src/app/api/youtube-channels/transcribe/route.ts` (initial completion + retries) so freshly generated summaries never enter the DB wrapped, and at read time as the first step of `normalizeSummaryHeadings()` so legacy summaries already in `video_transcriptions` render correctly without DB migration. Conservative regex — only matches when both opening and closing fences sit at the very start/end of the trimmed string; internal fenced blocks inside a longer summary are left untouched.\n\n**Topics on app mount.** Daily Summaries admin (and other topic-aware admin pages — Feeds, YouTube Channels, Stats) was loading with an empty `topics={topicLabels}` when the user navigated straight to it without first visiting `/app/articles`. Latent bug surfaced by the v1.109 routing reshuffle: the auth-redirect now lands signed-in users directly on `/app` (videos), and the menu shortcut to `/app/daily-summaries` skips the home page entirely. Two-line fix: drop the `if (currentPage !== \"home\") return` guard in `src/app/app/page.tsx` so `/api/topics` is fetched once at mount regardless of which SPA page the user lands on; `DailySummariesPage` aligns `selectedTopic` to the first topic when the parent's fetch resolves after the page is already mounted (race condition on direct navigation).\n\n**Release.** Bump 1.109.1 → 1.109.2 via `npm run release:patch`.",
    body_fr:
      "**Landing.** La section YouTube intelligence est restructurée en deux colonnes (kicker + titre + paragraphe descriptif à gauche, vrai screenshot produit d'un résumé YT 8news à droite) et passe au-dessus de la section How-it-works (Hero → Ticker → Stats → **YT** → How → Topics → Pricing → FAQ → CTA → Footer). Le screenshot vit dans `public/landing/yt-summary-preview.png` (`loading=\"lazy\"`) ; la section stack en colonne unique en dessous de 960 px. La sub du hero commence maintenant par les chaînes YouTube et mentionne les 400+ flux RSS en second ; la sub YT mentionne le flux bilingue intelligent (traduction EN→FR en moins de 10 s) et le nouveau téléchargement en un clic de la transcription complète (.txt) ou du résumé (.md). `#features` gagne un `border-top`. Description du `<head>` racine corrigée (`400++` → `400+`) et réordonnée pour matcher le copy du hero.\n\n**Stabilité de la transcription.** La route transcribe dépassait systématiquement le budget Netlify d'environ 30 s pour les longs podcasts (60 s+ en local), ce qui se traduisait en 502 opaques en prod. Trois fixes ciblés : (1) timeout OpenAI par requête (`OPENAI_TIMEOUT_MS = 20s`) via l'option `timeout` du SDK pour qu'un appel modèle ne puisse jamais bloquer la fonction ; (2) transcripts très longs (> 50 K caractères) échantillonnés en 32 K de tête + 14 K de queue avec un marqueur d'élision `[…]` — c'est typiquement les parties les plus riches en signal, le transcript complet est préservé jusqu'à ~30 min, seul le 1 h+ est tronqué, chaque troncature loggée avec `videoId` ; (3) la boucle de retry `compressSummaryIfNeeded` est court-circuitée (gardée dans le fichier pour réutilisation future) car chaque retry était un autre appel OpenAI de 10–20 s qui faisait sauter le mur des 30 s — le prompt impose déjà `SUMMARY_MAX_CHARS`, un résultat trop long est acceptable, un 502 ne l'est pas. Plus du polish opérationnel : `console.error` à chaque catch avec `videoId` + `lang` + message brut ; les patterns d'erreur courants mappés sur des statuts plus parlants (timeout/aborted → 504 + « Vidéo trop longue… », transcript 404 → 404 + « Pas de sous-titres exploitables », rate limit / 429 → 429 + « Limite atteinte… ») ; la réponse porte désormais à la fois `error` (chaîne utilisateur) et `raw` (debug).\n\n**Nettoyage des fences Markdown.** GPT enveloppe parfois une longue complétion Markdown dans un bloc code (``` ```markdown\\n…\\n``` ```), ce que `react-markdown` rend ensuite comme un unique `<pre><code>` monospace avec la syntaxe brute `## INTRO` / `**gras**` visible. Nouveau helper `stripCodeFences()` appliqué aux deux bouts : en écriture dans `src/app/api/youtube-channels/transcribe/route.ts` (complétion initiale + retries) pour que les résumés frais n'entrent jamais en base enveloppés, et en lecture comme première étape de `normalizeSummaryHeadings()` pour que les anciens résumés déjà en base se rendent correctement sans migration DB. Regex conservatrice — ne matche que quand les fences d'ouverture et de fermeture sont au tout début et à la toute fin de la chaîne trimée ; les blocs de code internes dans un résumé plus long sont laissés intacts.\n\n**Topics au mount de l'app.** L'admin Daily Summaries (et les autres admin pages topic-aware — Flux, Chaînes YouTube, Stats) chargeaient avec un `topics={topicLabels}` vide quand l'utilisateur y arrivait directement sans passer d'abord par `/app/articles`. Bug latent révélé par la refonte routing v1.109 : la redirection auth amène maintenant les owners directement sur `/app` (vidéos), et le raccourci menu vers `/app/daily-summaries` saute complètement la page home. Fix en deux lignes : retirer le garde `if (currentPage !== \"home\") return` dans `src/app/app/page.tsx` pour que `/api/topics` soit fetché une fois au mount peu importe la page SPA d'arrivée ; `DailySummariesPage` aligne `selectedTopic` sur le premier topic quand la résolution du fetch du parent arrive après le mount du composant (race condition sur navigation directe).\n\n**Release.** Bump 1.109.1 → 1.109.2 via `npm run release:patch`.",
    created_at: "2026-04-20T09:00:00Z",
  },
  {
    version: "1.109.1",
    title_en: "v1.109.1: Landing copy & layout polish, image logo, dev escape hatch, release tooling",
    title_fr: "v1.109.1 : Polish copy & layout landing, logo image, raccourci dev, tooling de release",
    body_en:
      "Landing polish: hero kicker repurposed from a build-version label into the brand tagline **Tech / AI / Crypto** with a larger, looser display style; headline copy reworded and capped at 64px so it sits cleanly on a single line in the wider 1.3fr copy column; subhead bumped to 400+ RSS feeds; root `<head>` metadata description updated accordingly. The text logo (gold serif `8news`) is replaced by `/logo-8news.png` in both `LandingNav` and `LandingFooter`, so the landing, the SPA and every SSR page now share a single brand asset; the sticky nav bar grows from 64px to 96px to give the image breathing room. The YouTube section is trimmed to its kicker / title / sub block (the two podcast preview cards are removed) and its title is constrained to a single line via `whiteSpace: \"nowrap\"`. Hero secondary CTA **Voir un résumé du jour / See a daily summary** now points to `/app/top-articles` (Top 50 du jour) instead of scrolling to the in-page Topics section. Dev escape hatch: **double-click the 8news logo** in the SPA header opens the marketing landing on `/?preview=1`, which the middleware now whitelists so authenticated maintainers can review the landing without signing out. Release tooling (Option A): `package.json` becomes the single source of truth for the version (semver `1.109.1`); new `scripts/release.mjs` propagates it to `public/version.json`, the SPA `APP_VERSION`, the landing pricing copy and the landing footer-bottom version line, idempotently and with stale-pattern warnings; `npm run release:patch | minor | major | sync` shortcuts wired in `package.json`. The UI keeps the compact display form (trailing `.0` stripped, so `1.109.0` → `v1.109` and `1.109.1` keeps its triplet). New `COMMITS.md` at the repo root documents the Conventional Commits convention, the type → semver mapping and the release workflow, paving the way for a future `release-please` GitHub Action.",
    body_fr:
      "Polish landing : le kicker du hero, qui affichait jusqu'ici un label de version build, devient la signature de marque **Tech / AI / Crypto** dans un style plus grand et plus aéré ; la headline est reformulée et plafonnée à 64px pour tenir proprement sur une seule ligne dans la colonne copy élargie (1.3fr) ; le sous-titre passe à 400+ flux RSS ; la metadata description du `<head>` racine est alignée. Le logo texte serif doré `8news` est remplacé par `/logo-8news.png` dans `LandingNav` et `LandingFooter`, donc landing, SPA et pages SSR partagent désormais le même asset de marque ; la sticky nav passe de 64px à 96px de hauteur pour donner de l'air à l'image. La section YouTube est resserrée à son bloc kicker / title / sub (les deux cartes podcast d'exemple sont retirées) et son titre est forcé sur une seule ligne via `whiteSpace: \"nowrap\"`. CTA secondaire du hero **Voir un résumé du jour / See a daily summary** : pointe maintenant vers `/app/top-articles` (Top 50 du jour) au lieu de scroller vers la section Topics interne. Raccourci dev : **double-clic sur le logo 8news** dans le header de la SPA ouvre la landing marketing sur `/?preview=1`, que le middleware whiteliste maintenant pour qu'un mainteneur connecté puisse revoir la landing sans se déconnecter. Tooling de release (Option A) : `package.json` devient la source unique de vérité pour la version (semver `1.109.1`) ; nouveau `scripts/release.mjs` qui la propage vers `public/version.json`, `APP_VERSION` de la SPA, le copy pricing landing et la ligne de version du footer landing, de façon idempotente et avec warnings sur regex obsolètes ; raccourcis `npm run release:patch | minor | major | sync` câblés dans `package.json`. L'UI garde la forme d'affichage compacte (le `.0` de fin est strippé, donc `1.109.0` → `v1.109` et `1.109.1` garde son triplet). Nouveau `COMMITS.md` à la racine documente la convention Conventional Commits, le mapping type → semver et le workflow de release, en vue d'une future GitHub Action `release-please`.",
    created_at: "2026-04-20T07:00:00Z",
  },
  {
    version: "1.109",
    title_en: "v1.109: Marketing landing on `/`, SPA moved to `/app`, FR default, fonts via next/font",
    title_fr: "v1.109 : Landing marketing sur `/`, SPA déplacée vers `/app`, FR par défaut, polices via next/font",
    body_en:
      "Major routing reshuffle to free up the root domain for the new marketing landing. The whole SPA (Videos page + Articles + Top 50 + Favorites + Daily Summaries + Topics admin + …) now lives under `/app`: every former pseudo-route (`/articles`, `/videos`, `/stats`, `/topics`, `/favorites`, `/daily-summaries`, `/top-articles`, `/summaries-browse`, `/youtube-channels`, `/feeds`, `/categories`, `/settings`, `/crons`, `/changelog`) becomes `/app/<path>`, with `PAGE_PATHS` + `pathToPage` updated and `next.config.ts` + `middleware.ts` rewriting the matching set to `/app`. SSR routes (`/{topic}`, `/{topic}/{date}/{slug}`, `/summaries`, `/api/**`) are strictly preserved — same paths, same content, no redirect chain. The middleware adds a strict-equality `pathname === \"/\" && user` 307 to `/app` so authenticated visitors land directly on the app and anonymous visitors see the landing. Internal links across `GeneralMenu`, `SeoNavBar` and the back-to-home `<Link>` of every SSR page are repointed to `/app`. New `src/app/page.tsx` is a Server Component assembling 11 landing components in `src/app/components/landing/` (Nav, Hero, Console, Ticker, Stats, How, Topics, YT, Pricing, FAQ, CTA, Footer). The FAQ is the only Client Component (accordion with keyboard support). Bilingual content lives in `src/lib/landing-content.ts` (typed `LandingContent`, single source of truth for both EN and FR text). Styles in `src/app/landing.css` are fully scoped under `.landing-root` with prefixed `landing-*` keyframes so nothing can leak to the SPA or SSR pages. **FR is now the default language** on `/` (no query param required); `?lang=en` switches to English via plain `<Link>` navigation. Geist + Instrument Serif + JetBrains Mono are wired through `next/font/google` in `layout.tsx` as CSS variables — only the landing actually uses them, the SPA + SSR keep their existing system stack from `globals.css`. The Topics mini-histograms use a deterministic mulberry32 PRNG seeded by index instead of `Math.random()` to avoid SSR hydration mismatches. All Try-it-now / Sign-in CTAs target `/app`; the daily-summary CTA targets `/summaries`. Static `metadata` ships title, description, OpenGraph, Twitter cards, canonical and `hreflang` en/fr. APP_VERSION + version.json bumped to 1.109; landing kicker and pricing copy aligned to v1.109.",
    body_fr:
      "Refonte majeure du routing pour libérer la racine du domaine au profit de la nouvelle landing marketing. Toute la SPA (Vidéos + Articles + Top 50 + Favoris + Résumés quotidiens + admin Topics + …) vit désormais sous `/app` : chaque ancienne pseudo-route (`/articles`, `/videos`, `/stats`, `/topics`, `/favorites`, `/daily-summaries`, `/top-articles`, `/summaries-browse`, `/youtube-channels`, `/feeds`, `/categories`, `/settings`, `/crons`, `/changelog`) devient `/app/<chemin>`, avec `PAGE_PATHS` + `pathToPage` mis à jour et `next.config.ts` + `middleware.ts` qui réécrivent l'ensemble vers `/app`. Les routes SSR (`/{topic}`, `/{topic}/{date}/{slug}`, `/summaries`, `/api/**`) restent strictement préservées — mêmes chemins, même contenu, aucune chaîne de redirection. Le middleware ajoute une règle d'égalité stricte `pathname === \"/\" && user` qui renvoie un 307 vers `/app`, donc les utilisateurs authentifiés atterrissent directement sur l'app tandis que les anonymes voient la landing. Tous les liens internes (`GeneralMenu`, `SeoNavBar`, les `<Link>` « retour accueil » de chaque page SSR) repointent vers `/app`. Le nouveau `src/app/page.tsx` est un Server Component qui assemble 11 composants landing dans `src/app/components/landing/` (Nav, Hero, Console, Ticker, Stats, How, Topics, YT, Pricing, FAQ, CTA, Footer). La FAQ est le seul Client Component (accordéon avec support clavier). Le contenu bilingue vit dans `src/lib/landing-content.ts` (interface typée `LandingContent`, source unique pour EN et FR). Les styles dans `src/app/landing.css` sont entièrement scopés sous `.landing-root` avec des keyframes préfixées `landing-*` pour qu'aucun débordement ne soit possible vers la SPA ou les pages SSR. **Le FR est désormais la langue par défaut** sur `/` (pas de paramètre requis) ; `?lang=en` bascule en anglais via simple navigation `<Link>`. Geist + Instrument Serif + JetBrains Mono sont branchées via `next/font/google` dans `layout.tsx` en CSS variables — seule la landing les utilise, la SPA + SSR conservent leur stack système existante via `globals.css`. Les mini-histogrammes des cards Topics utilisent un PRNG déterministe (mulberry32 seedé par index) au lieu de `Math.random()` pour éviter les hydration mismatches SSR. Tous les CTAs Try it now / Essayer / Sign in pointent vers `/app` ; le CTA secondaire vers `/summaries`. La `metadata` statique embarque title, description, OpenGraph, Twitter cards, canonical et `hreflang` en/fr. APP_VERSION + version.json passent à 1.109 ; kicker landing et copy pricing alignés sur v1.109.",
    created_at: "2026-04-20T03:00:00Z",
  },
  {
    version: "1.108",
    title_en: "v1.108: Inline YouTube embed, Summary popover (md/txt), Long/Short toggle, AI summary length cap with self-shortening",
    title_fr: "v1.108 : Lecteur YouTube embed, pop-up Résumé (md/txt), interrupteur Long/Short, plafond de longueur des résumés IA avec auto-raccourcissement",
    body_en:
      "Videos card title becomes **Vidéo du jour / Video of the day**; the action button is renamed **Résumé / Summary** (icon-only state changes: lines / spinner / check). Clicking the thumbnail or **Play Vidéo / Play Video** swaps the image for a YouTube `<iframe>` (`embed/<id>?autoplay=1&rel=0&modestbranding=1`, fullscreen + PiP allowed); once playing, the action button becomes **Ouvrir sur YouTube / Open on YouTube**. New **external-link icon** between Favorite and Download to open the video on youtube.com in a new tab. New `GET /api/youtube-channels/transcript?videoId=...` returns the raw transcript as `text/plain` with `Content-Disposition: attachment` and a slugified filename plus a small header (title, channel, date, link). New **DownloadTranscriptButton** opens a small upward-anchored popover (gold border + halo, ESC / outside-click to close, hover/disabled states, brief green-check confirmation) with two choices: *Résumé (.md)* downloads the cached AI summary and *Transcription complète (.txt)* hits the new endpoint. Shorts switch replaced by an exclusive **VideoKindToggle** (`Long | Short`, default **Long**) with adapted empty-state hints (i18n keys `videoKindLong`, `videoKindShorts`, `videoKindToggleAria`, `videoKindHintNoLong/NoShorts`). The audio player block now shows a small uppercase gold **Lecteur audio / Audio player** label with 10 px top padding. Transcription summary prompt (FR + EN): hard cap **5000 characters** insisted on at the top of the prompt and at the end (target 4500-4800 chars), with explicit instructions to **never end with `…` / `...`** and always finish on a complete sentence; `targetWords` rebalanced (200-300 / 450-650 / 650-800 words). Server safety net replaced: **no truncation anymore** — `compressSummaryIfNeeded` re-asks GPT (gpt-4.1-mini, up to 2 retries) to rewrite the summary shorter while keeping structure / tone / key facts; `stripTrailingEllipsis` cleans any leftover `…` from earlier runs. Translation prompt also enforces the 5000-char cap. Changelog API hides versions absent from `CHANGELOG_ENTRIES` and overrides title/body from code; **v1.106 restored** alongside the merged **v1.107** entry. APP_VERSION + version.json bumped to 1.108.",
    body_fr:
      "Le titre de carte de la page Vidéos devient **Vidéo du jour / Video of the day** ; le bouton d'action est renommé **Résumé / Summary** (l'icône change selon l'état : lignes / spinner / coche). Cliquer la miniature ou **Play Vidéo / Play Video** remplace l'image par un `<iframe>` YouTube (`embed/<id>?autoplay=1&rel=0&modestbranding=1`, plein écran + PiP autorisés) ; une fois la vidéo lancée, le bouton devient **Ouvrir sur YouTube / Open on YouTube**. Nouvelle **icône lien externe** entre Favori et Téléchargement, ouvre la vidéo sur youtube.com dans un nouvel onglet. Nouveau `GET /api/youtube-channels/transcript?videoId=...` qui renvoie la transcription brute en `text/plain` avec `Content-Disposition: attachment`, nom de fichier slugifié et petit en-tête (titre, chaîne, date, lien). Nouveau composant **DownloadTranscriptButton** qui ouvre un petit pop-up ancré vers le haut (cadre or + halo, fermeture par ESC ou clic extérieur, états hover/disabled, flash coche verte) avec deux choix : *Résumé (.md)* télécharge le résumé IA déjà chargé et *Transcription complète (.txt)* appelle le nouvel endpoint. L'ancien interrupteur Shorts est remplacé par un **VideoKindToggle** exclusif (`Long | Short`, défaut **Long**) avec des messages d'écran vide adaptés (clés i18n `videoKindLong`, `videoKindShorts`, `videoKindToggleAria`, `videoKindHintNoLong/NoShorts`). Le bloc lecteur audio affiche désormais un petit libellé doré majuscule **Lecteur audio / Audio player** avec 10 px de padding haut. Prompt de résumé de transcription (FR + EN) : plafond strict **5000 caractères** martelé en tête et en pied de prompt (cible 4500-4800 chars), avec instruction explicite de **ne jamais terminer par `…` / `...`** et toujours finir par une phrase complète ; `targetWords` rééquilibré (200-300 / 450-650 / 650-800 mots). Filet de sécurité serveur revu : **plus aucune troncature** — `compressSummaryIfNeeded` redemande à GPT (gpt-4.1-mini, jusqu'à 2 essais) de réécrire le résumé plus court en gardant structure / ton / faits clés ; `stripTrailingEllipsis` nettoie les éventuels `…` résiduels d'anciens passages. Le prompt de traduction impose aussi la limite 5000 caractères. La route changelog masque les versions absentes de `CHANGELOG_ENTRIES` et écrase titre/corps depuis le code ; **v1.106 restaurée** à côté de l'entrée fusionnée **v1.107**. APP_VERSION + version.json passent à 1.108.",
    created_at: "2026-04-20T00:00:00Z",
  },
  {
    version: "1.107",
    title_en: "v1.107: Videos as default home, Shorts toggle, transcription auto-open with slow reveal",
    title_fr: "v1.107 : Vidéos en accueil par défaut, interrupteur Shorts, ouverture auto du résumé avec dévoilement lent",
    body_en:
      "SPA routing: the Videos experience is now the app entry at `/` (default `currentPage`); the topic grid and AI news flow moved to `/articles`, with `/videos` still accepted for bookmarks. `next.config` rewrites and `middleware` SPA paths include `/articles` so hard refresh keeps session cookies. `GET /api/youtube-channels/videos` takes `?lang=en|fr` and returns `summaryMd` when a `video_transcriptions` row exists so cards load existing AI summaries without another transcribe call. Videos page: Shorts On/Off (default Off) filters out videos shorter than two minutes (`durationSec < 120`); the Transcription control always shows the word “Transcription” and only swaps the icon (lines vs check) when a summary exists; failed runs still show an error block with retry. After a successful **Transcription** run, the AI summary now **opens automatically** under the card (no extra click). The panel uses a **CSS grid `0fr` → `1fr` transition (~2.2s)** so lower cards **ease downward** and make room; inner content **fades in** with a short delay (~1.1s opacity). **Smooth `scrollIntoView`** nudges the expanded block into view after open. **Collapse** (button or “Réduire le résumé”) animates closed with the same slow height transition. **`prefers-reduced-motion: reduce`** short-circuits animations and skips animated scroll. Visible **dark-ring spinner** stays on the gold Transcription button while loading. General Menu (and `SeoGeneralMenu`): the Videos pill is leftmost, before Topics. Header subtitle is now **Tech • IA • Crypto** in English and French (`subtitle` in `i18n.ts`). House icon in the header highlights on Videos; logo reset returns to Videos.",
    body_fr:
      "Routing SPA : l’expérience Vidéos est désormais l’entrée de l’app sur `/` (`currentPage` par défaut) ; la grille des topics et le flux d’actualités IA sont sur `/articles`, avec `/videos` toujours accepté pour les favoris d’URL. Les rewrites `next.config` et les chemins SPA du `middleware` incluent `/articles` pour que le rechargement dur conserve les cookies de session. `GET /api/youtube-channels/videos` accepte `?lang=en|fr` et renvoie `summaryMd` lorsqu’une ligne `video_transcriptions` existe, pour hydrater les cartes sans relancer la transcription. Page Vidéos : interrupteur Shorts On/Off (défaut Off) masque les vidéos de moins de deux minutes (`durationSec < 120`) ; le bouton Transcription affiche toujours le mot « Transcription » et ne change que l’icône (lignes vs coche) lorsqu’un résumé existe ; en cas d’échec, un bloc d’erreur permet de réessayer. Après une **Transcription** réussie, le résumé IA **s’ouvre automatiquement** sous la carte (sans clic supplémentaire). Le panneau utilise une **transition CSS grid `0fr` → `1fr` (~2,2 s)** pour que les cartes du dessous **descendent en douceur** et libèrent la place ; le contenu **apparaît en fondu** avec un court délai (~1,1 s en opacité). Un **`scrollIntoView` fluide** recadre le bloc ouvert. Le **repli** (bouton ou « Réduire le résumé ») referme avec la **même transition de hauteur lente**. Le media **`prefers-reduced-motion: reduce`** désactive les animations et le défilement animé. **Spinner anneau sombre** visible sur le bouton or pendant le chargement. Menu général (et `SeoGeneralMenu`) : l’entrée Vidéos est la plus à gauche, avant Topics. Le sous-titre du header est désormais **Tech • IA • Crypto** en anglais et en français (`subtitle` dans `i18n.ts`). L’icône maison du header est active sur Vidéos ; le clic logo renvoie vers Vidéos.",
    created_at: "2026-04-19T22:00:00Z",
  },
  {
    version: "1.106",
    title_en: "v1.106: Videos as default home, General Menu order, videos UX, baseline",
    title_fr: "v1.106 : Vidéos en accueil par défaut, ordre menu général, UX vidéos, baseline",
    body_en:
      "SPA routing: the Videos experience is now the app entry at `/` (default `currentPage`); the topic grid and AI news flow moved to `/articles`, with `/videos` still accepted for bookmarks. `next.config` rewrites and `middleware` SPA paths include `/articles` so hard refresh keeps session cookies. `GET /api/youtube-channels/videos` takes `?lang=en|fr` and returns `summaryMd` when a `video_transcriptions` row exists so cards load existing AI summaries without another transcribe call. Videos page: Shorts On/Off (default Off) filters out videos shorter than two minutes (`durationSec < 120`); the Transcription control always shows the word “Transcription” and only swaps the icon (lines vs check) when a summary exists; the markdown summary stays collapsed until the user clicks the same button; a dark-ring spinner stays visible on the gold button while transcribing; failed runs still show an error block with retry. General Menu (and `SeoGeneralMenu`): the Videos pill is leftmost, before Topics. Header subtitle is now **Tech • IA • Crypto** in English and French (`subtitle` in `i18n.ts`). House icon in the header highlights on Videos; logo reset returns to Videos.",
    body_fr:
      "Routing SPA : l’expérience Vidéos est désormais l’entrée de l’app sur `/` (`currentPage` par défaut) ; la grille des topics et le flux d’actualités IA sont sur `/articles`, avec `/videos` toujours accepté pour les favoris d’URL. Les rewrites `next.config` et les chemins SPA du `middleware` incluent `/articles` pour que le rechargement dur conserve les cookies de session. `GET /api/youtube-channels/videos` accepte `?lang=en|fr` et renvoie `summaryMd` lorsqu’une ligne `video_transcriptions` existe, pour hydrater les cartes sans relancer la transcription. Page Vidéos : interrupteur Shorts On/Off (défaut Off) masque les vidéos de moins de deux minutes (`durationSec < 120`) ; le bouton Transcription affiche toujours le mot « Transcription » et ne change que l’icône (lignes vs coche) lorsqu’un résumé existe ; le résumé Markdown reste replié jusqu’au clic sur ce bouton ; un spinner anneau sombre reste lisible sur le bouton or pendant la transcription ; en cas d’échec, un bloc d’erreur permet de réessayer. Menu général (et `SeoGeneralMenu`) : l’entrée Vidéos est la plus à gauche, avant Topics. Le sous-titre du header est désormais **Tech • IA • Crypto** en anglais et en français (`subtitle` dans `i18n.ts`). L’icône maison du header est active sur Vidéos ; le clic logo renvoie vers Vidéos.",
    created_at: "2026-04-19T18:00:00Z",
  },
  {
    version: "1.105",
    title_en: "v1.105: Topics admin — inline category, General Menu wording",
    title_fr: "v1.105 : Admin Topics — catégorie inline, libellé menu général",
    body_en:
      "On the owner Topics admin list, each row now includes a category dropdown: changing it PATCHes only `categoryId` via `/api/topics/[id]`, with optimistic row updates, a per-row saving state, and rollback plus `topicCategorySaveError` if the request fails. The General Menu entry previously labeled “Articles” is now “Topics” in both English and French (`generalMenuArticlesBtn`).",
    body_fr:
      "Sur la liste admin Topics (owner), chaque ligne propose un menu déroulant de catégorie : le changement envoie un PATCH limité à `categoryId` via `/api/topics/[id]`, avec mise à jour optimiste, état « en cours » par ligne, et restauration + message `topicCategorySaveError` en cas d'échec. Dans le menu général, l'entrée auparavant intitulée « Articles » s'affiche désormais « Topics » en anglais et en français (`generalMenuArticlesBtn`).",
    created_at: "2026-04-19T12:00:00Z",
  },
  { version: "1.104", title_en: "v1.104: Top 50 bullet persistence, no auto-refresh on Top 50 page", title_fr: "v1.104 : Persistance bullets Top 50, plus de refresh auto sur la page Top 50", body_en: "Top 50 AI summary bullet points are now persisted into the `summary_bullets` table with `source_type = 'top50'`. Each bullet is stored once per distinct topic found in its referenced articles. Idempotent: existing top50 bullets for the same lang+date are replaced on each generation. Client now passes `topic` per article to the API. New `insertTopSummaryBullets` Supabase helper handles delete+insert. Fire-and-forget persistence does not slow down the API response. Also disabled the 5-minute background polling on the Top 50 page that was causing the summary to reset into loading state.", body_fr: "Les bullet points du résumé IA Top 50 sont désormais persistés dans la table `summary_bullets` avec `source_type = 'top50'`. Chaque bullet est stocké une fois par topic distinct trouvé dans ses articles référencés. Idempotent : les bullets top50 existants pour la même langue+date sont remplacés à chaque génération. Le client envoie maintenant le `topic` par article à l'API. Nouveau helper Supabase `insertTopSummaryBullets` pour le delete+insert. La persistance en fire-and-forget ne ralentit pas la réponse API. Désactivation également du polling de fond toutes les 5 minutes sur la page Top 50 qui causait le retour en état loading du résumé.", created_at: "2026-04-17T06:00:00Z" },
  { version: "1.103", title_en: "v1.103: Favorites — video AI summary link, audio player", title_fr: "v1.103 : Favoris — lien résumé vidéo IA, lecteur audio", body_en: "On My Favorites, video favorites now show a discrete “transcription” link after the date when a `video_transcriptions` row exists for the current UI language. Clicking expands the stored Markdown summary (react-markdown); a small “collapse” control closes it. Lazy fetch via new `GET /api/video-transcription`. Favorites list API accepts `?lang=` and returns `videoId` + `hasTranscription` (batch lookup). When expanded, the usual ElevenLabs `AudioPlayer` appears above the TL;DR block, using the same TTS text prep and user speed/voice as the Videos page.", body_fr: "Sur Mes favoris, les favoris vidéo affichent un lien discret « transcription » après la date lorsqu'une ligne `video_transcriptions` existe pour la langue d'interface courante. Un clic déplie le résumé Markdown stocké (react-markdown) ; un petit contrôle « replier » le referme. Chargement à la demande via `GET /api/video-transcription`. L'API liste des favoris accepte `?lang=` et renvoie `videoId` + `hasTranscription` (requête groupée). Une fois déplié, le `AudioPlayer` ElevenLabs habituel apparaît au-dessus du bloc TL;DR, avec la même préparation de texte TTS et les réglages vitesse/voix que sur la page Vidéos.", created_at: "2026-04-17T04:00:00Z" },
  { version: "1.102", title_en: "v1.102: Audio player for video summaries, Play Video button", title_fr: "v1.102 : Lecteur audio pour résumés vidéo, bouton Play Vidéo", body_en: "Added ElevenLabs audio player (same AudioPlayer component as article summaries) to each video transcription summary — plays TTS with user-preferred speed and voice (FR/EN). Text composed from intro + cleaned Markdown content. Added 'Play Video' button with play icon below video metadata, opening the YouTube video in a new tab.", body_fr: "Ajout du lecteur audio ElevenLabs (même composant AudioPlayer que les résumés d'articles) sur chaque résumé de transcription vidéo — lecture TTS avec vitesse et voix préférées (FR/EN). Texte composé d'une intro + contenu Markdown nettoyé. Ajout du bouton « Play Vidéo » avec icône play sous les métadonnées, ouvrant la vidéo YouTube dans un nouvel onglet.", created_at: "2026-04-17T02:00:00Z" },
  { version: "1.101", title_en: "v1.101: Topic assignment for channels/videos, fix video bullet points, topic propagation", title_fr: "v1.101 : Attribution de topic aux chaînes/vidéos, fix bullet points vidéo, propagation de topic", body_en: "Added topic_id column to youtube_channels, youtube_videos, and video_transcriptions tables — each channel can now be assigned a topic from the admin page (dropdown selector). Topic propagates automatically: channel → video (on RSS upsert) → transcription (on transcribe). Fixed critical bug where video transcription bullet points were silently failing to insert into summary_bullets (missing topic_id NOT NULL and refs columns). Fixed multi-line bullet extraction: only the first line of each bullet was captured, now the full paragraph is included. Made topic_id nullable in summary_bullets for video source_type. Added PATCH endpoint for youtube_channels to update topic. Daily summary background cron now uses static imports for reliable Netlify bundling.", body_fr: "Ajout de la colonne topic_id sur youtube_channels, youtube_videos et video_transcriptions — chaque chaîne peut être associée à un topic depuis la page admin (sélecteur déroulant). Le topic se propage automatiquement : chaîne → vidéo (lors de l'upsert RSS) → transcription (lors de la transcription). Correction d'un bug critique où les bullet points des transcriptions vidéo échouaient silencieusement à l'insertion dans summary_bullets (topic_id NOT NULL et refs manquants). Correction de l'extraction multi-lignes des bullets : seule la première ligne était capturée, maintenant le paragraphe complet est inclus. Rendu topic_id nullable dans summary_bullets pour le source_type vidéo. Ajout d'un endpoint PATCH pour youtube_channels pour mettre à jour le topic. Le cron background des daily summaries utilise désormais des imports statiques pour un bundling Netlify fiable.", created_at: "2026-04-17T00:00:00Z" },
  { version: "1.100", title_en: "v1.100: Video duration via YouTube API, responsive mobile layout, auth gate, daily summary background cron", title_fr: "v1.100 : Durée vidéo via YouTube API, layout mobile responsive, accès authentifié, cron daily summary en background", body_en: "Video durations now fetched automatically from YouTube Data API v3 (free, batched up to 50 per call) and cached in youtube_videos.duration_sec — displayed as '12m 13s' on each card. Publication time always shown in 12h AM/PM format. Video cards now responsive: horizontal layout on desktop, vertical stacking (thumbnail above, text below) on mobile (≤640px). Videos page visible in the General Menu for all users; unauthenticated users see a sign-in prompt with lock icon and 'Free registration' message. Daily summary cron converted to Netlify background function (cron-daily-summary-background.ts) for instant 202 response, fixing cron-job.org timeouts. General Menu spacing increased (+20px margin).", body_fr: "Durée des vidéos récupérée automatiquement via YouTube Data API v3 (gratuit, par lots de 50) et mise en cache dans youtube_videos.duration_sec — affichée en « 12m 13s » sur chaque carte. Heure de publication toujours au format 12h AM/PM. Cartes vidéo responsive : layout horizontal sur desktop, empilement vertical (miniature au-dessus, texte en dessous) sur mobile (≤640px). Page vidéos visible dans le Menu Général pour tous ; les utilisateurs non connectés voient une invitation à se connecter avec icône cadenas et mention « Inscription gratuite ». Cron daily summary converti en Netlify background function (cron-daily-summary-background.ts) pour réponse 202 instantanée, corrigeant les timeouts cron-job.org. Espacement du Menu Général augmenté (+20px de marge).", created_at: "2026-04-16T22:00:00Z" },
  { version: "1.99", title_en: "v1.99: YouTube videos page, AI transcription summaries, channel management", title_fr: "v1.99 : Page vidéos YouTube, résumés IA de transcriptions, gestion des chaînes", body_en: "New YouTube video monitoring system: admin page to manage YouTube channels (@handle or URL, auto-resolved via TranscriptAPI). Videos page accessible to all users via the General Menu, displaying videos from configured channels with date navigation (prev/next arrows + calendar picker). Video metadata cached in a new youtube_videos table for past-date lookups. One-click AI transcription per video: fetches transcript from TranscriptAPI (JSON segments parsed to clean text, timing stripped), generates a structured Markdown summary (## TL;DR + ## Key Points) via GPT-4.1-mini, stored as raw Markdown in video_transcriptions table. Cross-language optimization: if a summary exists in the other language, translates it instead of re-transcribing (saves 1 API credit + ~80% tokens). Markdown rendered client-side via react-markdown (dynamic import, SSR disabled). Video cards show truncated description with 'See more', channel name, date, time, duration (computed from transcript segments after first transcription), and view count. Collapsible summary display. summary_bullets table extended with source_type ('article'/'video') and optional video_transcription_id FK. Channel metadata auto-refreshed on admin page load with retry logic (@handle fallback). SPEC.md updated.", body_fr: "Nouveau système de monitoring vidéo YouTube : page admin pour gérer les chaînes YouTube (@handle ou URL, résolution automatique via TranscriptAPI). Page vidéos accessible à tous via le Menu Général, affichant les vidéos des chaînes configurées avec navigation par date (flèches précédent/suivant + calendrier). Métadonnées vidéo mises en cache dans une nouvelle table youtube_videos pour les recherches passées. Transcription IA en un clic par vidéo : récupération du transcript via TranscriptAPI (segments JSON parsés en texte brut, timing supprimé), génération d'un résumé Markdown structuré (## TL;DR + ## Points clés) via GPT-4.1-mini, stocké en Markdown brut dans la table video_transcriptions. Optimisation cross-langue : si un résumé existe dans l'autre langue, traduction au lieu de re-transcrire (économie d'1 crédit API + ~80% de tokens). Rendu Markdown côté client via react-markdown (import dynamique, SSR désactivé). Cartes vidéo avec description tronquée et « Voir plus », nom de chaîne, date, heure, durée (calculée depuis les segments après première transcription), nombre de vues. Résumé dépliable. Table summary_bullets étendue avec source_type ('article'/'video') et video_transcription_id FK optionnel. Métadonnées des chaînes auto-rafraîchies au chargement de la page admin avec logique de retry (fallback @handle). SPEC.md mis à jour.", created_at: "2026-04-16T18:00:00Z" },
  { version: "1.98", title_en: "v1.98: General menu, dedicated Top 50 page, owner crown icon, Supabase RLS security", title_fr: "v1.98 : Menu général, page Top 50 dédiée, icône couronne owner, sécurité RLS Supabase", body_en: "Major navigation refactoring: extracted a persistent General Menu (Home, My Favorites, Top 50 articles, Daily Summaries) visible on all pages, replacing the previous per-page button bar. 'Edit My Topics' moved to a separate secondary row on the homepage with a distinct dashed style. Top 50 articles is now a dedicated standalone page (/top-articles) that auto-triggers analysis on navigation — removed topic toggle, period picker, and edit controls from that view. Daily Summaries button now uses SPA navigation (no full page reload) via a new client-side SummariesBrowsePage component and /api/summaries/routes endpoint, eliminating flickering. Added the General Menu (SeoGeneralMenu) to all SEO pages (/summaries, topic hubs, individual summaries). Admin menu items (Topics, Categories, Feeds, Daily Summaries, Videos) merged into the user dropdown menu for owners, removing the separate shield icon. Owner users now display a crown icon on the user menu. Added visual calendar date picker (MiniCalendar) to the admin Daily Summaries generator page. Videos placeholder page added. SPA URL routing extended with pushState/popstate for all new pages. Enabled Row Level Security on all 8 unprotected Supabase tables (topics, feeds, articles, news_cache, changelog, categories, user_topic_preferences, user_favorites) with service_role-only policies.", body_fr: "Refactoring majeur de la navigation : extraction d'un Menu Général persistant (Accueil, Mes Favoris, Top 50 articles, Résumés quotidiens) visible sur toutes les pages, remplaçant la barre de boutons précédente. « Modifier mes topics » déplacé sur une ligne secondaire distincte sur l'accueil avec un style pointillé. Top 50 articles est maintenant une page dédiée (/top-articles) qui lance automatiquement l'analyse à la navigation — suppression de la grille des topics, du sélecteur de périodes et des contrôles d'édition de cette vue. Le bouton Résumés quotidiens utilise désormais la navigation SPA (sans rechargement complet) via un nouveau composant SummariesBrowsePage et l'endpoint /api/summaries/routes, éliminant le flickering. Ajout du Menu Général (SeoGeneralMenu) sur toutes les pages SEO (/summaries, hubs topic, résumés individuels). Items du menu admin (Topics, Catégories, Flux, Résumés quotidiens, Videos) fusionnés dans le dropdown utilisateur pour les owners, suppression de l'icône bouclier séparée. Les owners affichent maintenant une icône couronne sur le menu utilisateur. Ajout d'un date picker calendrier visuel (MiniCalendar) dans la page admin de génération des Daily Summaries. Page placeholder Videos ajoutée. Routing URL SPA étendu avec pushState/popstate pour toutes les nouvelles pages. Activation de Row Level Security sur les 8 tables Supabase non protégées (topics, feeds, articles, news_cache, changelog, categories, user_topic_preferences, user_favorites) avec des politiques service_role-only.", created_at: "2026-04-15T18:00:00Z" },
  { version: "1.97", title_en: "v1.97: Fix daily summary date filter, sanitize OpenAI payloads, breadcrumb nav", title_fr: "v1.97 : Correction filtre date daily summaries, sanitisation payloads OpenAI, fil d'Ariane", body_en: "Fixed critical bug in daily summary generation where getScoredArticles had no upper date bound: articles from future days consumed the LIMIT 50 slots, leaving only ~12 articles for the target day. Now queries use a strict [dayStart, dayEnd] range on fetched_at. Switched all daily summary date filtering from pub_date to fetched_at for consistency. Added sanitization of all text sent to OpenAI (titles, snippets, prompts) to strip control characters that caused 400 JSON parse errors for ~10% of topics. Added 'Summaries' link in breadcrumb navigation on topic hub and individual summary SEO pages.", body_fr: "Correction d'un bug critique dans la génération des daily summaries : getScoredArticles n'avait pas de borne supérieure de date, les articles des jours suivants consommaient les 50 places du LIMIT, ne laissant que ~12 articles pour le jour cible. Les requêtes utilisent désormais un intervalle strict [dayStart, dayEnd] sur fetched_at. Passage de tout le filtrage de date des daily summaries de pub_date à fetched_at pour la cohérence. Ajout de la sanitisation de tous les textes envoyés à OpenAI (titres, snippets, prompts) pour supprimer les caractères de contrôle qui causaient des erreurs 400 JSON parse pour ~10% des topics. Ajout du lien « Summaries » dans le fil d'Ariane des pages hub topic et summary SEO individuelles.", created_at: "2026-04-15T14:00:00Z" },
  { version: "1.96.2", title_en: "v1.96.2: Audio player for daily summaries, period picker extended, UI polish", title_fr: "v1.96.2 : Lecteur audio résumés quotidiens, sélecteur de période étendu, polish UI", body_en: "Added audio news player on daily summary SEO pages and the summaries explorer, composing TTS from bullet points with user-preferred speed and voice. Extended the homepage period picker with 2 mo and 3 mo buttons, with slightly reduced button sizing so all 13 options fit on one line. Simplified the /summaries 'Browse a summary' section to a single-line form (topic + date + View link) that opens the full page in the current tab, removing inline summary display. Aligned baseline text flush with the logo left edge across all screen sizes. Increased seo_title character limit to prevent truncation.", body_fr: "Ajout du lecteur audio de news sur les pages SEO de résumés quotidiens et l'explorateur, composant le TTS à partir des bullet points avec vitesse et voix préférées. Extension du sélecteur de période de l'accueil avec les boutons 2 mo et 3 mo, taille des boutons légèrement réduite pour tenir sur une seule ligne. Simplification de la section « Parcourir un résumé » de /summaries en formulaire sur une seule ligne (topic + date + lien View) ouvrant la page complète dans l'onglet courant, sans affichage inline du résumé. Alignement de la baseline au bord gauche du logo sur toutes les tailles d'écran. Augmentation de la limite de caractères du seo_title pour éviter la troncature.", created_at: "2026-04-15T10:00:00Z" },
  { version: "1.96.1", title_en: "v1.96.1: SPEC.md update, changelog sync, action bar spacing", title_fr: "v1.96.1 : Mise à jour SPEC.md, sync changelog, espacement barre d'actions", body_en: "Updated SPEC.md with all changes from v1.92 through v1.96 (favorites system, topic categories, SEO daily summary pages, explorer page, hybrid rendering). Added v1.96 changelog entry. Increased action bar bottom margin for better visual spacing between the personalization bar and the topic grid.", body_fr: "Mise à jour de SPEC.md avec tous les changements de v1.92 à v1.96 (système de favoris, catégories de topics, pages SEO de résumés quotidiens, page explorateur, rendu hybride). Ajout de l'entrée changelog v1.96. Augmentation de la marge basse de la barre d'actions pour un meilleur espacement visuel entre la barre de personnalisation et la grille des topics.", created_at: "2026-04-14T22:00:00Z" },
  { version: "1.96", title_en: "v1.96: Daily summaries explorer, SEO best practices, gpt-4.1-mini upgrade", title_fr: "v1.96 : Explorateur de résumés quotidiens, best practices SEO, passage à gpt-4.1-mini", body_en: "New public /summaries page with topic selector and date picker to browse daily summaries. Page is hybrid SSR+client: crawlable topic grid with recent summary links for Google, plus interactive explorer. SEO footer added to root layout with links to /summaries and sitemap.xml. Action bar (Customize, Analyze, Daily Summaries) moved above topic grid. Switched daily summary AI model from gpt-4.1-nano to gpt-4.1-mini with 50 articles for richer, more reliable content. Enhanced AI prompts for detailed bullets with specific numbers, anecdotes, and surprising facts. Articles limited to top 10 per summary for SEO. JSON parse retry on malformed responses.", body_fr: "Nouvelle page publique /summaries avec sélecteur de topic et date picker pour consulter les résumés quotidiens. Page hybride SSR+client : grille de topics crawlable avec liens vers les derniers résumés pour Google, plus explorateur interactif. Footer SEO ajouté au layout racine avec liens vers /summaries et sitemap.xml. Barre d'actions (Personnaliser, Analyser, Résumés quotidiens) déplacée au-dessus de la grille de topics. Passage du modèle IA de gpt-4.1-nano à gpt-4.1-mini avec 50 articles pour un contenu plus riche et fiable. Prompts IA enrichis pour des bullet points détaillés avec chiffres, anecdotes et faits surprenants. Articles limités à 10 par résumé pour le SEO. Retry JSON en cas de réponse malformée.", created_at: "2026-04-14T18:00:00Z" },
  { version: "1.95", title_en: "v1.95: SEO daily summary pages with AI entity extraction", title_fr: "v1.95 : Pages de résumés quotidiens SEO avec extraction d'entités IA", body_en: "Added public SEO-optimized daily summary pages per topic at /[topic]/[date]/[slug]. Each page is server-rendered with AI-generated bullets, relevant articles, JSON-LD structured data, hreflang tags, and Open Graph metadata. Summaries are generated via a new POST /api/summaries/generate endpoint (owner or cron) and stored in daily_summaries + summary_bullets tables. Individual bullets include AI-extracted named entities (GIN-indexed) for future semantic features. New admin page 'Daily Summaries' allows manual generation per topic/date or batch all topics. Topic hub pages list all summaries with pagination. Dynamic sitemap.xml covers all generated pages.", body_fr: "Ajout de pages de résumés quotidiens SEO par topic à /[topic]/[date]/[slug]. Chaque page est rendue côté serveur avec des bullet points IA, articles pertinents, données structurées JSON-LD, balises hreflang et métadonnées Open Graph. Les résumés sont générés via un nouveau endpoint POST /api/summaries/generate (owner ou cron) et stockés dans les tables daily_summaries + summary_bullets. Chaque bullet inclut des entités nommées extraites par IA (indexées GIN) pour de futures fonctionnalités sémantiques. Nouvelle page admin « Résumés quotidiens » pour la génération manuelle par topic/date ou en lot. Pages hub par topic avec pagination. Sitemap.xml dynamique couvrant toutes les pages générées.", created_at: "2026-04-14T14:00:00Z" },
  { version: "1.94", title_en: "v1.94: Article favorites system, button text updates, admin menu reorder", title_fr: "v1.94 : Système de favoris articles, textes boutons, réordonnancement menu admin", body_en: "Added a full article favorites system: star icon on every article (optimistic toggle, auth guard), dedicated Favorites page accessible from the header (authenticated users only), backed by a new user_favorites table, API route (GET/POST/DELETE), and useFavorites hook. The star appears on ArticleCard, TopFeedSection, AllArticlesTab, and StatsPage. Updated 'Analyze top articles' button text to include '50'. Reordered admin menu: Topics, Categories, Feed management.", body_fr: "Ajout d'un système complet de favoris articles : icône étoile sur chaque article (toggle optimiste, guard auth), page Favoris dédiée accessible depuis le header (utilisateurs connectés uniquement), appuyé par une nouvelle table user_favorites, route API (GET/POST/DELETE) et hook useFavorites. L'étoile apparaît sur ArticleCard, TopFeedSection, AllArticlesTab et StatsPage. Mise à jour du texte du bouton « Analyse des top articles » pour inclure « 50 ». Réordonnancement du menu admin : Topics, Catégories, Gestion des flux.", created_at: "2026-04-14T10:00:00Z" },
  { version: "1.93.1", title_en: "v1.93.1: Member post-create redirect and validation toast on home", title_fr: "v1.93.1 : Redirection member après création et toast de validation sur l'accueil", body_en: "Fixed member-only post-create flow for topics: after submitting the new topic form, members are now redirected back to the homepage (since they do not have access to the Topics admin list). A validation toast is displayed on home to confirm the topic is pending approval with a maximum 24-hour delay.", body_fr: "Correction du flux post-création pour les members : après validation du formulaire de nouveau topic, les members sont désormais redirigés vers la page d'accueil (car ils n'ont pas accès à la liste admin des topics). Un toast de confirmation s'affiche sur l'accueil pour indiquer que le topic est en cours de validation avec un délai maximum de 24 heures.", created_at: "2026-04-13T21:00:00Z" },
  { version: "1.93", title_en: "v1.93: RSS creation UX refactor with hidden draft flow", title_fr: "v1.93 : Refonte UX création RSS avec brouillon caché", body_en: "Refactored topic creation UX with a dedicated RSS box: AI discovery and manual feed addition are now grouped in a clearer flow. Added hidden draft topic orchestration so the first RSS action creates the topic once, then reuses it for discover/manual feed operations without losing form context. Improved error handling and validation messages for draft prerequisites. On Create, the UI now returns to the topics list and shows a validation toast indicating up to 24 hours for approval. Included related polish and wiring updates across Topics page/list and i18n labels.", body_fr: "Refonte de l'UX de création de topic avec une box Flux RSS dédiée : découverte IA et ajout manuel de flux sont maintenant regroupés dans un flux plus clair. Ajout d'une orchestration de brouillon caché : la première action RSS crée le topic une seule fois, puis le réutilise pour la découverte IA et l'ajout manuel sans perte de contexte du formulaire. Amélioration des messages d'erreur et de validation des prérequis du brouillon. Au clic sur Créer, l'interface revient désormais sur la liste des topics et affiche un toast de validation indiquant un délai maximum de 24 heures. Inclus aussi les ajustements connexes sur Topics/list et les libellés i18n.", created_at: "2026-04-13T20:00:00Z" },
  { version: "1.92", title_en: "v1.92: Fix topic sort order on creation", title_fr: "v1.92 : Correction de l'ordre des topics à la création", body_en: "Fixed a bug where new topics could appear before existing ones because sort_order was computed from displayed topics only (ignoring inactive/hidden ones). Now uses all topics to determine the next sort_order. Added migration 009 to re-sequence existing sort_order values without duplicates.", body_fr: "Correction d'un bug où les nouveaux topics pouvaient s'afficher avant les existants car le sort_order était calculé uniquement sur les topics affichés (ignorant les inactifs/masqués). Utilise désormais tous les topics pour déterminer le prochain sort_order. Ajout de la migration 009 pour re-séquencer les valeurs sort_order existantes sans doublons.", created_at: "2026-04-13T18:00:00Z" },
  { version: "1.91", title_en: "v1.91: Baseline update, unified topic creation and member feed access", title_fr: "v1.91 : Nouvelle baseline, création de topic unifiée et accès feeds pour les membres", body_en: "EN baseline changed to \"Tech decoded by AI\". Topic creation flow unified for owners and members: same form, same 5-second toast notification on validation (pending approval within 24h). Removed the blocking message-only view for members. Members can now discover RSS feeds via AI and add feeds manually, same as owners. \"Edit my topics\" button replaces the previous badge + edit button. Subtitle alignment adjusted per language. Obsolete files removed (insert-changelog SQLs, planning docs, rock.html).", body_fr: "Baseline EN changée en « Tech decoded by AI ». Flux de création de topic unifié owners/members : même formulaire, même toast 5 secondes à la validation (en attente d'approbation sous 24h). Suppression de l'écran bloquant pour les members. Les members peuvent désormais découvrir des feeds RSS par IA et ajouter des feeds manuellement, comme les owners. Bouton « Modifier mes topics » remplace l'ancien badge + bouton modifier. Alignement du sous-titre ajusté par langue. Fichiers obsolètes supprimés (SQL insert-changelog, docs de planning, rock.html).", created_at: "2026-04-13T16:00:00Z" },
  { version: "1.90", title_en: "v1.90: Auto-sync changelog from code to database", title_fr: "v1.90 : Synchronisation automatique du changelog code vers base de données", body_en: "Changelog entries are now defined in src/lib/changelog-entries.ts (TypeScript). The GET /api/changelog route automatically inserts any missing versions into the database on first call after deploy. No more manual SQL copy-paste in Supabase needed for new releases — just add an entry to the array and deploy.", body_fr: "Les entrées du changelog sont désormais définies dans src/lib/changelog-entries.ts (TypeScript). La route GET /api/changelog insère automatiquement les versions manquantes dans la base au premier appel après déploiement. Plus besoin de copier/coller du SQL dans Supabase pour les nouvelles releases — il suffit d'ajouter une entrée au tableau et de déployer.", created_at: "2026-04-13T12:00:00Z" },
  { version: "1.89", title_en: "v1.89: Topic categories, admin menu, user menu, prompt and UI polish", title_fr: "v1.89 : Catégories de topics, menu admin, menu utilisateur, prompt et polish UI", body_en: "Added topic categories system (Technology, Health, Sport) with a dedicated categories table, API endpoint, and selectors in topic creation and editing. Replaced separate Topics and Feeds nav icons with a single admin star-icon dropdown (owner-only). Added a user icon dropdown for sign-in/sign-out, removing the sign-out button from the header bar. Enriched the top-summary AI prompt with anecdotes and concrete data requirements. Summary metadata line changed to full grey. Onboarding modal filtered to Technology category. Various subtitle alignment and topic display fixes.", body_fr: "Ajout du système de catégories de topics (Technologie, Santé, Sport) avec table dédiée, endpoint API et sélecteurs dans la création et l'édition des topics. Remplacement des icônes séparées Topics et Flux par un menu déroulant unique étoile admin (owner uniquement). Ajout d'une icône utilisateur avec menu déroulant connexion/déconnexion, suppression du bouton déconnexion de la barre header. Enrichissement du prompt résumé top avec exigences d'anecdotes et données concrètes. Ligne méta du résumé passée en gris. Modal d'onboarding filtrée sur la catégorie Technologie. Corrections d'alignement sous-titre et d'affichage des topics.", created_at: "2026-04-13T10:00:00Z" },
  { version: "1.88", title_en: "v1.88: Specialized background functions and external scheduling", title_fr: "v1.88 : Fonctions background spécialisées et scheduling externe", body_en: "Specialized background functions: fetch-background is now fetch-only (no mini-scoring), score-background is score-only with raised caps (150 articles/run, 300 hard cap, aggressive adaptive scaling). Scoring stamps last_scored_at before processing to prevent double-scoring from concurrent runs. Removed scheduled trigger functions (cron-fetching, cron-scoring) and their netlify.toml schedule declarations — scheduling is now handled externally via cron-job.org calling the background function endpoints directly.", body_fr: "Spécialisation des fonctions background : fetch-background est désormais fetch-only (sans mini-scoring), score-background est score-only avec des caps relevés (150 articles/run, 300 hard cap, scaling adaptatif agressif). Le scoring pose last_scored_at avant le traitement pour empêcher le double-scoring en cas d'exécutions concurrentes. Suppression des scheduled triggers (cron-fetching, cron-scoring) et de leurs déclarations de schedule dans netlify.toml — le scheduling est désormais externalisé via cron-job.org appelant directement les endpoints des background functions.", created_at: "2026-04-13T06:00:00Z" },
  { version: "1.87", title_en: "v1.87: Background functions architecture and multi-pass cron optimization", title_fr: "v1.87 : Architecture background functions et optimisation cron multi-pass", body_en: "Migrated cron jobs from scheduled functions (30s limit) to a scheduled-trigger + background-function architecture with 15-minute runtime. Scheduled functions (cron-fetching, cron-scoring) now act as lightweight triggers that POST to background functions carrying the full logic. Fetch background processes all active topics in a multi-pass loop, re-checking staleness each pass and running extended mini-scoring (up to 80 articles) after each topic. Score background runs a multi-pass loop that re-queries backlogs and keeps scoring until all are drained or budget is exhausted, with fair per-topic budget distribution. Cron schedule changed from every minute to every 10 minutes. Fixed Cron Monitor timeline to use fetched_at instead of pub_date for activity buckets.", body_fr: "Migration des crons depuis les scheduled functions (limite 30s) vers une architecture trigger schedulé + background function avec 15 minutes de runtime. Les scheduled functions (cron-fetching, cron-scoring) deviennent des déclencheurs légers qui POST vers des background functions contenant toute la logique. Le fetch background traite tous les topics actifs en boucle multi-pass, re-vérifiant la fraîcheur à chaque passe et exécutant un mini-scoring étendu (jusqu'à 80 articles) après chaque topic. Le score background tourne en boucle multi-pass qui re-query les backlogs et continue le scoring jusqu'à vidange complète ou épuisement du budget, avec répartition équitable du budget par topic. Cadence cron changée de chaque minute à toutes les 10 minutes. Correction du tableau d'activité Cron Monitor pour utiliser fetched_at au lieu de pub_date.", created_at: "2026-04-13T02:00:00Z" },
  { version: "1.86", title_en: "v1.86: Cron rollback, member submission UX and scheduled functions fix", title_fr: "v1.86 : Retour cron, UX soumission membre et fix fonctions schedulées", body_en: "Restored previous cron runtime tuning (13s baseline) in fetch/score functions and removed 30s production overrides from netlify.toml for stability. Improved member topic-submission UX: after non-owner creation, show a message-only pending-validation view with a dedicated back-to-home action (no form shown). Refined home personalization controls so the top-analysis button stays globally available but is hidden during authenticated topic-edit mode. Added explicit [functions] directory declaration in netlify.toml to fix scheduled functions not triggering after deploy with the Next.js plugin.", body_fr: "Rétablissement du paramétrage cron précédent (baseline 13s) dans les fonctions fetch/score et suppression des overrides production 30s dans netlify.toml pour plus de stabilité. Amélioration UX de soumission de topic côté membre : après création non-owner, affichage d'un écran message-only de validation en attente avec action dédiée de retour à l'accueil (sans formulaire). Ajustement des contrôles de personnalisation home : le bouton d'analyse top reste globalement disponible mais est masqué pendant le mode édition des topics pour un utilisateur connecté. Ajout de la déclaration explicite [functions] dans netlify.toml pour corriger le non-déclenchement des fonctions schedulées après déploiement avec le plugin Next.js.", created_at: "2026-04-12T22:00:00Z" },
  { version: "1.85", title_en: "v1.85: On-demand top analysis, member topic submission and cache stability", title_fr: "v1.85 : Analyse top à la demande, soumission de topic membre et stabilité cache", body_en: "Homepage top articles analysis is now user-triggered via a dedicated button (\"Analyze top articles\"), with contextual toasts for topic/period guidance and updated wording (\"selected and analyzed by AI\"). Signed-in members can propose new topics from home personalization; created topics are stored inactive/hidden pending owner validation, with a 24h max validation notice. Improved action-button visual consistency and hardened top-summary cache hit stability (deterministic ordering + normalized cache key).", body_fr: "L'analyse des top articles sur l'accueil devient déclenchée à la demande via un bouton dédié (« Analyse des top articles »), avec toasts de guidance topic/période et wording mis à jour (« sélectionnés et analysés par IA »). Les membres connectés peuvent proposer un nouveau topic depuis la personnalisation home ; les topics créés sont enregistrés inactifs/masqués en attente de validation owner, avec message de validation sous 24h max. Amélioration de la cohérence visuelle des boutons d'action et fiabilisation du cache de résumé top (ordre déterministe + clé normalisée).", created_at: "2026-04-12T20:00:00Z" },
  { version: "1.84", title_en: "v1.84: Personalization UX + cron 30s budget + summary/count polish", title_fr: "v1.84 : Personnalisation topics + budget cron 30s + polish résumé", body_en: "Added signed-in user topic personalization (selection, onboarding modal, per-user persistence, and homepage top-feed filtering by selected topics). Upgraded Netlify cron runtime budgeting to align with 30s server window (fetch + score budgets and production env defaults). Refined summary metadata wording for selected-topic mode (shows analyzed count) and polished the \"Customize my topics\" controls.", body_fr: "Ajout de la personnalisation des topics pour les utilisateurs connectés (sélection, onboarding, persistance par utilisateur et filtrage du flux top de l'accueil selon les topics choisis). Passage des budgets de runtime cron Netlify au nouveau plafond serveur 30s (fetch + score + variables de prod). Ajustement du libellé méta du résumé en mode topic sélectionné (affichage du nombre analysé) et amélioration UI des contrôles « Personnaliser mes topics ».", created_at: "2026-04-12T12:00:00Z" },
  { version: "1.83", title_en: "v1.83: Top 50 homepage + AI grouped summary + display filtering", title_fr: "v1.83 : Top 50 accueil + résumé IA groupé + filtre affichage", body_en: "Homepage Top feed now uses 50 articles over 24h. Topics include a new is_displayed toggle; hidden topics stay active for ingestion/scoring but are excluded from homepage display and Top feed selection. Added POST /api/news/top-summary for homepage AI summary with grouped bullet points and source references, plus audio playback in the same SummaryBox experience. UI updates include AI Summary label, Top 50 subtitle styling, progressive reveal animation for homepage summary, and loading indicator text.", body_fr: "Le flux Top de l'accueil passe à 50 articles sur 24h. Les topics ont un nouveau toggle is_displayed ; les topics masqués restent actifs pour l'ingestion/scoring mais sont exclus de l'affichage accueil et de la sélection Top. Ajout de POST /api/news/top-summary pour un résumé IA de la home avec bullet points groupés et références sources, avec lecture audio dans la même expérience SummaryBox. Mises à jour UI : libellé Résumé IA, style du sous-titre Top 50, animation d'affichage progressive du résumé sur la home et texte de chargement.", created_at: "2026-04-11T12:00:00Z" },
  { version: "1.82", title_en: "v1.82: My Account, Users management, Top 20 timestamp, GA & AI labels", title_fr: "v1.82 : Mon compte, gestion utilisateurs, timestamp Top 20, GA & labels IA", body_en: "Settings: My Account section for authenticated users (editable first/last name, read-only email and user type badge). Users management for owner users (inline edit name and type via service-role API). Homepage: removed manual refresh button; added last-updated timestamp on Top 20 subtitle (HH:MM). Baselines updated: EN \"Tech intelligence, powered by AI.\" / FR \"La tech décodée par l'IA\". Topic creation reorganized: Label EN, Label FR, Slug on one row; Domain moved up; new \"Generate with AI\" button fills slug, label FR and domain from label EN via /api/topics/generate-labels. Cron status: score age only flags slow/high when backlog > 0. Google Analytics (G-X8RR3FMCR0) integrated in layout.tsx.", body_fr: "Paramètres : section Mon compte pour les utilisateurs connectés (prénom/nom modifiables, e-mail et type en lecture seule). Gestion des utilisateurs pour les owners (modification inline nom et type via API service-role). Accueil : bouton refresh supprimé ; horodatage de mise à jour sur le sous-titre Top 20 (HH:MM). Baselines mises à jour : EN « Tech intelligence, powered by AI. » / FR « La tech décodée par l'IA ». Création de topic réorganisée : Label EN, Label FR, Slug sur une ligne ; Domain remonté ; bouton « Generate with AI » remplit slug, label FR et domain depuis le label EN via /api/topics/generate-labels. Statut cron : l'âge du scoring ne déclenche slow/high que si le backlog > 0. Google Analytics (G-X8RR3FMCR0) intégré dans layout.tsx.", created_at: "2026-04-10T12:00:00Z" },
  { version: "1.81", title_en: "v1.81: Member vs owner roles for admin screens", title_fr: "v1.81 : rôles member et owner pour les écrans admin", body_en: "user_type in Supabase user_metadata: member (default at sign-up) or owner. Only owner can open Topics and Feed management; members stay on the same public areas as guests. Admin APIs use requireOwnerSession(): 401 if not signed in, 403 if member. Promote to owner in Supabase Dashboard → Authentication → Users → User metadata (user_type = owner); user must sign in again to refresh the JWT. src/lib/user-type.ts.", body_fr: "user_type dans les métadonnées utilisateur Supabase : member (défaut à l'inscription) ou owner. Seul owner accède à Topics et à la gestion des flux ; les members gardent les mêmes zones publiques que les invités. Les API admin utilisent requireOwnerSession() : 401 si non connecté, 403 si member. Passer en owner dans le dashboard Supabase → Authentication → Users → métadonnées (user_type = owner) ; nouvelle connexion pour rafraîchir le JWT. Fichier src/lib/user-type.ts.", created_at: "2026-04-16T12:00:00Z" },
  { version: "1.80", title_en: "v1.80: Supabase sign-in for Topics & Feed management", title_fr: "v1.80 : connexion Supabase pour Topics et gestion des flux", body_en: "Optional Supabase Auth (email + password). Guests keep full access to home, stats, crons, changelog, and settings. Topics and Feed management (nav icons + APIs) require a session; APIs return 401 without valid cookies. @supabase/ssr, middleware.ts session refresh, AuthProvider + AuthModal; registration stores first/last name in user_metadata. GET /api/topics without ?all=1 stays public for the homepage topic grid.", body_fr: "Authentification Supabase optionnelle (e-mail + mot de passe). Les invités gardent l'accès complet à l'accueil, stats, crons, changelog et paramètres. Les pages Topics et gestion des flux (icônes + API) exigent une session ; les API répondent 401 sans cookies valides. @supabase/ssr, middleware pour les cookies, AuthProvider + AuthModal ; inscription avec prénom/nom en user_metadata. GET /api/topics sans ?all=1 reste public pour la grille des sujets.", created_at: "2026-04-08T12:00:00Z" },
  { version: "1.0–1.48", title_en: "Early development (1.0–1.48)", title_fr: "Développement initial (1.0–1.48)", body_en: "Earlier 8news.ai development before detailed changelog (see git history). Versions 1.49+ summarized in SPEC.md (section 17).", body_fr: "Développement 8news.ai antérieur au journal détaillé (voir historique git). Versions ≥1.49 résumées dans SPEC.md (section 17).", created_at: "2026-03-13T12:00:00Z" },
];
